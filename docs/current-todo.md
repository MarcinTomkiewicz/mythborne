# Monster Hunt - Current TODO

Updated: 2026-06-04

This file lists pending work derived from:
- `docs/project-context.md`
- `docs/database-current.md`
- `docs/current-decisions.md`
- `docs/ui-ux-notes.md`

Order reflects implementation priority, not final business priority.

## Current Codex Backlog Position

- UI/UX foundation accepted: UI-CORE-1 - Mythsworn UI style contract extraction, UI-CORE-2 - Global SCSS and shared pattern inventory, UI-CORE-3 - Local SCSS budget and style report checklist, UI-CORE-4 - Shared surface/card/badge/chip/page-header patterns, UI-CORE-5 - Icon placeholder and Game Icons mapping contract, UI-CORE-6 - Item popover shared component contract, UI-CORE-7 - Legacy Monster Hunt / `mg-*` SCSS modernization plan, UI-CORE-8 - Text utility semantics and `muted-text` cleanup, UI-CORE-9 - Surface/card/badge/chip production pattern expansion, UI-CORE-10 - Custom icons and brand asset registry, UI-CORE-11 - Prototype-to-production SCSS mapping, UI-CORE-12 - PrimeNG/vendor wrapper modernization and lookup order, UI-CORE-13 - Utility class audit, semantics and usage pass, UI-CORE-14 - PrimeNG table/paginator/list pattern decision, UI-CORE-15 - Layout utilities and section pattern cleanup, and UI-CORE-CARD-SELECTED-1 - global selected/featured card state stabilized for hover/focus and item popover triggers.
- UI/UX foundation docs UI-CORE-1 through UI-CORE-15 are accepted. UI-SHELL-00 through UI-SHELL-02 are accepted as no-code shell preflight/status artifacts in `docs/ui-ux/ui-shell-00-02-inventory.md`, covering README-first lookup, current shell rollback boundary and token/color anchor gaps. UI-SHELL-1 - Game shell style foundation remains conditionally accepted. UI-SHELL-04 is accepted: authenticated shell containment is hardened, guest/account/public leakage is removed from the game shell, hard-refresh bootstrap restores selected server and selected hero context, and the sidebar server/hero switch copy is Polish. UI-SHELL-05 is accepted: shell viewport/content containment is stabilized with main-only scrolling, route content remains in `mg-container`, and the sidebar does not get a separate scrollbar. UI-SHELL-06 is accepted with follow-up: shell boundary surfaces now use explicit shell structure classes and current theme tokens; final color/elevation matching is deferred to UI-CORE-16 / later color pass. UI-SHELL-07 is accepted as no-code topbar mapping in `docs/ui-ux/ui-shell-07-topbar-anchor-mapping.md`. UI-SHELL-08 is accepted with follow-up: `app-shell` now owns only topbar placement while `app-game-topbar` owns normal content and staff fallback composition. UI-SHELL-09 is accepted: the normal topbar uses existing 3-column grid utilities for full-width brand centering without new SCSS/BEM classes; responsive overlap/wrap refinement remains deferred with UI-SHELL-18. UI-SHELL-10 is accepted: player and staff notification bells are right-zone items in `game-topbar`, with no parallel `app-shell` fallback. UI-SHELL-11 is postponed: topbar keeps `current / max` HP format but temporarily renders `max / max` until a canonical current-HP read model is scoped. UI-SHELL-12B is accepted: Health/XP topbar visuals now use the shared `mg-chip` skin primitive, existing flex/gap/width utilities and a single smooth `GameBar` progress pattern with theme-backed progress tokens. UI-SHELL-13 is accepted: topbar resources use the existing active-hero `hero_resources` read path for amounts/per-hour, shared core resource display definitions for Drachma/Materials/Workforce, and the current live amount calculation without showing Character Points as a produced resource. UI-SHELL-14 is accepted as fallback cleanup only: resource fallback chips kept icon, label, strong value and rate using existing classes, but did not implement final styling. UI-SHELL-15 is accepted with user patch: final topbar resources are compact right-side summaries with icon, readable label, strong value and rate through the global `mg-resource-summary*` shell pattern, with no `tag-badge` resource fallback, no `mg-chip--resource`, no local component SCSS and no TS/resource semantics/DB/RPC/generated-type changes. UI-SHELL-17 is accepted with user patch: topbar color/elevation uses semantic `mg-game-shell__topbar`, a token-based dark gradient, the existing shell gold border and `shadow-shell` elevation backed by dark-theme shadow tokens, without brittle selectors, local component SCSS or TS/DB/RPC/generated-type changes. UI-SHELL-18 is deferred / not implemented: the responsive-only utility patch was reverted and manual responsive smoke plus any targeted fix remain for a later pass. Later shell implementation should use the 00-02 artifact before runtime changes, and UI-SHELL-23+ should resolve the shared sidebar nav item / active nav pattern debt. HOTFIX-COMBAT-LIVE is accepted; next task will be selected by user.
- UI-EXPLORATION initial movement follow-up is accepted: `Start exploration` now uses backend-owned `start_or_get_hero_exploration_and_start_initial_step(...)` with `p_request_id`, fresh explorations enter `activeStep`, direction board remains hidden while an active step exists, and edge/backtrack movement contracts remain separate. Pending smoke: double-click Start idempotency, existing active-step timer recovery and existing ready-route `movementOptions[]` rendering.
- UI-EXPLORATION-10-FOLLOW-UP is accepted: the ready-route direction board now uses a centered gameplay/oracle board composition with Polish core copy, fixed board geometry, subtle rays, route-choice gates and a disabled Back placeholder when no real backtrack exists, while preserving backend-owned `movementOptions[]`, edge/backtrack validation and RPC semantics. User visual smoke is accepted enough for this pass; hover/glow polish is deferred unless tester feedback or broader shared-card polish requires it.
- UI-EXPLORATION-11 is accepted: active challenge runtime now uses a single source-aware challenge handoff/report surface, sandbox tools and diagnostics are sandbox-gated, success feedback is toast-based, resolved-step handoff pieces live under `src/app/game/components`, and combat challenge UI preserves the accepted live boundary: `ensure_exploration_combat_session(...)` / `get_combat_live_state(...)` recovery, Walking Dead when DB awaits player input with a timing manifest, `submit_combat_player_action(...)`, `get_combat_result_detail(...)` after completion and exact reward reads. Report actions now render after reward, public report copy uses `/report/:publicToken` only when the step/combat payload or existing report detail exposes a public token/link, and Trial/Encounter combat labels are source-aware. Exploration UI must not use `advance_combat_live_to_next_player_action` or add a frontend combat continuation/resolution. Follow-ups: richer backend-provided report copy/narratives, public/external report token coverage where backend payload lacks it, and later visual/prototype refinement if manual smoke asks for it.
- UI-EXPLORATION-12 follow-ups are accepted: pending active movement now renders a shared presentational Time Oracle timer surface in the existing exploration runtime shell, with full-width outer card, centered circular oracle, coherent countdown/progress source, central pending/ready card, and a single ready-state `Sprawdź wynik` action. Direction/result/report cards remain hidden while the step is pending; result checking still uses the existing backend workflow readiness/action path. The direction-board continuation has one shared board/footer block and a secondary `Zmień poziom trudności` action that returns to Difficulty preview only when no active step/challenge is running, without DB/RPC flow. Exploration combat now has a secondary `Rozstrzygnij automatycznie` action for combat encounters/ambushes and combat Trials only, using the dedicated `auto_resolve_exploration_combat_challenge_attempt(...)` RPC through typed service/mappers; non-combat Trial auto-resolve, PvP, manual Walking Dead, and the no-`advance_combat_live_to_next_player_action` boundary remain unchanged. Manual smoke remains user-side pending for representative pending/ready steps, Hard duration behavior, sandbox `Skróć czas`, the footer return action, and combat auto-resolve for Encounter combat plus Trial combat.
- UI-EXPLORATION-13 is accepted: the broad legacy `ExplorationStatusSection` was removed and runtime composition is split into focused primary, direction and diagnostics blocks. Direction board/header/difficulty-return predicates are owned by `ExplorationPageState`; diagnostics remain sandbox-gated; no DB/RPC/generated-type or local SCSS changes were added. Manual smoke remains user-side pending for the full no-exploration/runtime/pending/result/challenge/direction/reward matrix.
- UI-EXPLORATION-14 is accepted: the final Exploration control pass cleaned player-facing Difficulty, Trial detail, start and movement copy into Polish while preserving accepted entry/runtime composition, Time Oracle, direction board, challenge, reward/report and combat boundaries. Sandbox diagnostics remain gated and technical; no DB/RPC/generated-type, local SCSS, new component or gameplay-flow changes were added. Manual smoke remains user-side pending for the full Exploration matrix.
- UI-COMBAT-1 and UI-COMBAT-2 are superseded by the accepted UI-COMBAT-3 integration: the prototype `/game/combat` shell and sandbox-only resolver/harness code have been removed as a product surface, while the accepted left combatant / center action / right opponent layout now lives on the real Exploration/Trial live combat surface with live participant HP and stat cards.
- UI-COMBAT-3 is accepted: Exploration/Trial combat now renders the Combat Minigame Prototype structure with participant HP/stat panels, central Walking Dead timing, player-facing submit copy, completed/no-manifest states and separate auto-resolve. Walking Dead keeps the existing live combat timing flow and submits only timing input; no resolver/RPC/generated-type/damage/outcome/log/report flow was changed. The meter SCSS/HTML cleanup is accepted with shared timing mixins, radius/opacity utilities, reduced-motion handling and no local timing engine. Participant stat cards render backend-provided `baseStatRows` and `combatStatRows`; base stat value coloring uses backend `tone` plus `colorableFinalValue`, with no local stat calculation, label maps, Fatum dedupe/move or `rawJson` / `snapshot_json` parsing.
- UI-COMBAT-4 is accepted: Exploration/Trial active combat now renders a player-facing live combat log under the combat panels, with utility/template-based actor/body/result rows, safe attack-source emphasis, miss/evade outcomes kept in the result column with info text, and hit/crit/heal tone paths through existing utility classes. No local `combat-log__*` SCSS remains, and the accepted final cleanup did not change generated types, DB/RPC, Walking Dead, Manual/Auto or preview flow. Follow-ups: user-side visual/manual smoke may still tune small preferences; backend/Migrator owns combat resolver/read-model issues such as successful hit events with `0 obrażeń` and runtime stat discrepancies between dashboard, preview and combat; extract a shared combat/report log row pattern later if this row structure repeats.
- UI-COMBAT-5 is accepted: active and completed combat now use the shared combat stage/surface path, completed report participant cards keep visible base/combat stat rows, combat logs keep the accepted actor/body/result structure with safe source emphasis, and JSON report text extraction uses shared `json-display-text` helpers without exported generic combat-report `...keys` APIs. Exploration remains the outcome/reward/action wrapper, while combat owns only combat presentation.
- UI-COMBAT-6 is accepted: `CombatLogRow` is now the narrow presentational renderer for `CombatDisplayLogRow`, while `CombatLogPanel` owns only combat log groups/rounds and delegates individual rows. Active and completed logs keep the accepted actor/body/result structure, source emphasis, utility-driven tones and no local combat-log SCSS; no mapper, DB/RPC, generated type, Walking Dead, participant card or report-flow changes were made.
- UI-COMBAT-7 is accepted: completed combat reports keep rewards below the shared combat timeline, use the exploration reward handoff/display path, and keep report actions separate for internal full report and public copy/share. The full-report action uses the completed challenge `gameReportId` when present; public copy loads private report detail through `GameReports.getActiveHeroReportDetail(reportId)` when the combat payload does not already expose a public path, maps `publicToken` to `/report/{publicToken}`, and keeps missing token/path as a disabled warning state without fabricating URLs. The misleading same-route return action was removed. Follow-ups: visual polish for the report action section, and backend/read-model completion of missing auto-resolve participant `baseStatRows` / `combatStatRows` if still present.
- UI-COMBAT-8 and UI-COMBAT-8-Followup are accepted: Exploration routes combat challenges through `MinigameHost`, `CombatHost` stays thin, `CombatHostState` owns runtime/facade flow through `CombatSessions`, and active combat keeps the existing `CombatStage` / `CombatSurface` / `CombatLogPanel` / `CombatLogRow` path with the old `CombatDisplayLogRow` actor/body/result contract. Backend structured live event fields are mapped into that existing row shape; no new active `combat-report-log.mapper.ts` path, renderer contract, local parser, outcome inference, damage/healing construction or fake frontend report/reward data is accepted. Manual completion now hands off the completed combat pointer to Exploration, Exploration creates the accepted backend combat report pointer through `GameReportProducers.createCombatReportFromResult(...)`, refreshes completed challenge/reward context, and both manual and auto combat paths render the completed combat report surface from backend report detail. Completed combat report headers use semantic outcome tones: hero victory `success`, hero defeat `danger`, encounter draw `warning`, and trial draw `danger`. Verification passed with `npx tsc --noEmit`, `npm run build` with known budget/CommonJS warnings, and `git diff --check`; user manual smoke is OK.
- UI-COMBAT-9 is accepted as the final Combat UI polish/closure pass. No additional combat UI code changes were needed after the UI-COMBAT-8 manual smoke; the active backlog index now stops at UI-COMBAT-9, and the stale UI-COMBAT-10-12/PvP auto-resolve direction was replaced with an explicit boundary. PvP auto-resolve/report flow, PvP privacy/snapshot expansion, broader backend copy contracts and shared extraction return only as separate UI-PVP / PvP combat or targeted cleanup tasks. Verification for this docs-only pass: `git diff --check` passed with line-ending warnings only.
- UI-SHELL-19 is accepted: sidebar context uses the shared/global `mg-context-card` pattern with Hero, selected server/status and Prestige in one compact card with separators, no separate sidebar `M` block, no local sidebar SCSS and no TS/DB/RPC/generated-type/data semantics changes. Manual visual smoke remains user-side.
- UI-SHELL-20 is accepted: sidebar context data uses `ActiveServer` for selected server/status and the DB-backed `get_hero_prestige_public_summary` read model for Prestige, with stale hero/server responses cleared/ignored, `takeUntilDestroyed` cleanup, shared `humanizeKey` reuse and local menu URL helpers left as a sidebar IA cleanup candidate. Manual smoke remains user-side.
- UI-SHELL-21 is accepted: sidebar navigation IA is grouped from `menu-config.ts` into Hero, World and Operations, `Guild` lives under World, `Reports` remains under World because `/game/reports` owns Reports/Notifications tabs, access-policy filtering and active route behavior are preserved, all links target existing routes, and final nav visuals remain deferred. Manual smoke remains user-side.
- UI-SHELL-22 is accepted: the temporary `mg-card` sidebar nav fallback is replaced by the shared `mg-shell-nav-item` shell pattern, with centralized hover/focus/active styling, a visible left active inset, accepted custom icon classes (`pi pi-helmet`, `pi pi-skills`, `pi pi-hydra`, `pi pi-trade`), no local sidebar SCSS and no route/access/DB/RPC/generated-type semantics changes. Manual smoke remains user-side.
- UI-SHELL-23 is accepted: `mg-shell-nav-item` has a visible token-based `:focus-visible` ring while preserving the active left inset and centralized hover/focus/active shell pattern; `.mg-game-shell { min-height: 100dvh; }` is accepted as the current scrollable shell baseline. Manual visual/a11y smoke remains user-side.
- UI-SHELL-24 is accepted with user-side icon adjustments: logged-in sidebar menu icons use existing custom/project `pi pi-*` registry classes, including `pi pi-capitol` and `pi pi-trail`, with no broken image fallback, local sidebar SCSS, route/access, DB/RPC or generated-type semantics changes. Manual smoke remains user-side.
- UI-SHELL-25 is accepted: selected-server non-live/unavailable status now uses `tag-badge--warn` instead of muted, membership suspension timing is a warning badge, and membership block reason is strong text instead of muted helper copy; no new badge variants, local sidebar SCSS, route/access, DB/RPC or generated-type semantics changes were added. Manual smoke remains user-side.
- UI-SHELL-26 is accepted: shell surface palette alignment now uses token-level/global shell changes, with tuned dark shell tokens, `.mg-game-shell` consuming the theme background gradient, shell cards/context surfaces using shared shell surface tokens, and topbar/context/brand elevations consuming `v.$shadow-shell`, `v.$shadow-premium` and `v.$shadow-brand-mark`; no templates, TS, local component SCSS, DB/RPC or generated-type changes were introduced. Manual visual smoke remains user-side.
- UI-BACKGROUNDS-1 is accepted: main game route backgrounds now use the shared app shell/main content mechanism with `--mg-route-background-image` on `.mg-game-shell__main`, mapping Dashboard, Attributes, Exploration, Armory, Mansion and Vicinity to existing `public/images/backgrounds/` assets. Sidebar/topbar are not background targets, no per-page background SCSS was added, and DB/RPC/generated types were not touched. Follow-up: later UI polish may add a shared `mg-card`/surface opacity or transparency variant for route-background contexts.
- UI-DESIGN global card opacity pass is accepted: default `.mg-card` and ordinary game-shell card surfaces now use more transparent `color-mix(...)` backgrounds in `src/scss/base/_surface.scss`, while premium/header/dashboard-hero surfaces remain stronger. No element-level `opacity`, feature-local SCSS, templates, DB/RPC, generated types, specs or status-flow changes were added. Manual visual smoke remains user-side.
- UI-ESTATE-1 is accepted: `/game/estate` now uses the v3 Estate page context from `get_player_estate_page_context(p_hero_id)` and renders the accepted overview slice with header summary district/building count/active job/Vicinity entry, building current/next effect rows, build time, costs and basic build action through DB-owned eligibility fields. `/game/estate` was added as canonical Estate route support by aliasing the existing Mansion page; `/game/mansion` cleanup or legacy navigation decisions remain later route/IA work. Follow-ups: DB/RPC copy should remove the trailing period from `copyJson.empty.activeJob` instead of Angular trimming it; image slot aspect ratio polish is deferred; requirements/prestige/full disabled reasons remain UI-ESTATE-4, and district grouping remains UI-ESTATE-2. Manual smoke is user-side only.
- UI-ESTATE-3 is accepted: the Estate active building job panel now has live countdown/progress, due-job settlement through canonical `finalize_hero_estate_building_jobs(...)`, Estate context/dashboard reload after settlement, toast-based action errors and split Estate runtime row mappers. Estate start/finalize paths use the Estate page context hero id and no longer call `ActiveHero.loadActiveHero()` or direct-read `hero`; `.spec.ts` files stayed untouched. Follow-ups: fix `toBuildingDurationLabel` player-facing `sec` copy, investigate `get_player_estate_page_context` performance with user-side Network timings if loading remains slow, and handle building requirements/prestige/full disabled reasons in the next Estate task.
- UI-ENTRY-22-2A, UI-ENTRY-22-2B, UI-ENTRY-22-3, UI-ENTRY-22-4, UI-ENTRY-22-5 and UI-ENTRY-22-8 are accepted as the current public landing/account-entry/hero-creation/login entry baseline. `/` keeps the shared route-background path, centered Mythsworn banner, top-aligned desktop hero copy, main CTAs and right info cards; homepage content is centralized in `src/app/core/config/home-page-content.config.ts` with interfaces in `src/app/core/interfaces/home-page-content.interface.ts`. The reusable `AppFooter` lives under `src/app/layout/components/app-footer`, uses config from `src/app/core/config/app-footer.config.ts`, renders only on non-game/non-admin shell routes, and links only to existing routes/aliases (`/`, `/auth/login`, `/register`, `/auth/server-entry`). `/auth/server-entry`, `/auth/create-character` and `/auth/login` share the `ENTRY_BACKGROUND_IMAGE` route-background mapping and account-entry shell, use existing `mg-card` / `mg-data-row` / utility surfaces, and no longer have `_account-entry.scss` or `mg-account-entry-*` selectors. Account entry and game shell sidebars now share `ShellSidebarContent`, with project-owned sidebar contracts in `src/app/core/interfaces/layout/sidebar.interface.ts`, game nav config in `src/app/core/config/menu-config.ts`, and account-entry nav/context config in `src/app/core/config/account-entry-sidebar.config.ts`; guest login sidebar hides logout. Hero creation keeps the canonical create-hero workflow while create-character config/display constants live in `src/app/core/config/create-character-server-options.config.ts`, shared summary rows use `AccountEntrySummaryRow`, and server/origin blocking loads use shared `LoadingOverlay`. Login reuses `Auth.login`, `AuthState`, `authEntryGuard`, `LoginForm`, `FormFields` and `LOGIN_FIELDS`, redirects to `/auth/server-entry`, and links registration through `/register`; manual smoke remains user-side. `Stwórz bohatera` continues through `/auth/server-entry`; no DB/RPC, guards, generated types, start-flow, route-background, dashboard counters or mobile/responsive behavior changed.
- UI-ENTRY-22-6 is accepted with pending user smoke: `/` landing CTA config lives in `src/app/core/config/home-page-cta.config.ts` and `PublicHomePage` only switches guest/authenticated CTA sets from `AuthState.user()` when auth has been initialized by auth-required routes/services. Guest routes go to `/auth/login` and `/auth/register`; authenticated routes go through `/auth/server-entry`. Landing still does not resolve server/hero context.
- UI-ENTRY-22-7 is accepted with pending user smoke: entry surfaces received the minimal responsive/accessibility pass, including small-viewport account-entry shell stacking, explicit aria labels for server/hero selects, and visible focus styling for origin carousel nav buttons. Data-backed checks for server eligibility, existing hero entry and blocked/full server reasons remain user-side.
- UI-ENTRY-22-9 is accepted with pending user smoke: `/auth/register` is the real account registration panel, `/register` redirects there, footer/login links no longer treat hero creation as registration, and shared `FormFields` now supports PrimeNG Password with toggle-mask icons plus config-driven strength feedback. Account registration remains account-only and separate from `/auth/create-character`; no DB/RPC gameplay writes, generated types, guards, hero creation workflow or status-flow changes were added.
- UI-ENTRY-22-5-FOLLOW-UP is accepted with pending user smoke: `/auth/create-character` now keeps the selected world summary in the right column above `Pochodzenie bohatera` / `Następny krok`, moves the main hero creation flow upward, and constrains the hero-name field with existing utilities. Hero-name availability after blur/debounce is deferred until a canonical DB/RPC/read-model contract exists.
- UI-ENTRY-22-10 is accepted with pending user smoke: auth-entry links keep clear semantics, `/login` and `/register` remain aliases to `/auth/login` and `/auth/register`, public report return links point to `/`, and `Stwórz bohatera` continues through `/auth/server-entry`. Shared sidebar nav now supports `activeRoute` so `Wejdź do gry` and `Stwórz bohatera` no longer share the same active state on account-entry routes.
- UI-ENTRY no-server follow-up is accepted with pending user smoke: `/auth/server-entry` and `/auth/create-character` now render no visible/available servers as normal account-entry empty states with disabled selects, `Brak dostępnych serwerów` copy and clear player-facing messages. Server-entry loading uses shared `LoadingOverlay`, touched legacy utilities were cleaned, auth active-hero bootstrap resolves no selected server as normal `null` state, and create-character submit is blocked by `canSubmitHeroCreation()` before `CreateHero.createHero()` when no selected/creatable server exists. No direct table reads, RLS grants, DB/RPC changes, generated type changes or route redirects were added.
- UI-ENTRY auth-entry active-hero follow-up is accepted with pending user smoke: explicit `/auth/server-entry` `Wejdź do gry` now uses `select_account_entry_active_hero_context(p_server_id, p_hero_id)` and applies the returned context to `ActiveHeroState`, while auth initialization/login/listener no longer hydrate active hero or call `ActiveHero.loadActiveHero()` / `loadHeroRows()` / `TABLES.hero`. Protected-route refresh/restore remains a DB/RPC follow-up for a player-safe active-hero restore read model; do not reintroduce direct `public.hero` reads.
- UI-ENTRY start-flow create eligibility follow-up is accepted with pending user smoke: existing-account `/auth/create-character` now sends the exact `serverId` from `selectedServerAvailability()` / `get_start_flow_server_availability()` to `create_hero_start_flow(...)`, and `CreateHero.createHero(characterName, originId, serverId)` no longer resolves server state locally or calls `ActiveHero.selectHero()` after creation. Post-create active hero state is applied from the `create_hero_start_flow` result through `ActiveHero.applyStartFlowHeroCreationResult(...)`, avoiding direct `hero` table reads in the create-submit handoff. Creation eligibility is driven by DB `canCreateHero`; when false, UI uses DB `blockReason` / `eligibilityJson.createBlockReason` instead of frontend-inferred blockers.
- UI-ENTRY warning-cleanup follow-up is accepted with pending user smoke: root `@defer` was removed, `GlobalToast` now lives in both `AppShell` and `GameShell`, account-entry select disabled state is handled by reactive `FormControl.disable/enable`, and the create-character account step uses a real `<form>` around password fields. `NG0913` for the banner image is the only accepted remaining console warning and stays deferred to the later asset/storage follow-up.
- UI-ENTRY storage asset follow-up is accepted with pending browser smoke: large route backgrounds, origin artwork, paperdolls and district-A building images now resolve through centralized Supabase Storage URL helpers in `src/app/core/config/storage-assets.config.ts`, using the public `assets` bucket and fixed transform sizes. Small critical local assets such as banner/logo/favicon/icons remain local. Origin carousel images use `NgOptimizedImage` fill mode inside `.carousel-image__slot` with `object-fit: contain`, while central width/height metadata is kept for non-fill paperdoll and building preview/card images only.
- Player attributes page-context migration is accepted with user-side smoke in progress: `/hero/attributes` now uses `get_player_attributes_page_context(p_hero_id)` and `attributeManifest.allocationModel` for stat rows, costs, caps, draft totals, save eligibility and safe derived preview descriptors. Do not reintroduce direct reads of `hero_stats`, `stats`, `balance_formula_*` or `entity_formula_assignments`; keep durable writes on `save_stat_allocation(...)`. Remaining follow-ups: finish player page-context migration for other player routes in separate slices, keep derived stat display labels DB/read-model-owned where possible, and keep blocker reasons out of inline table layout except as tooltips/action affordances.
- Topbar XP stale-state follow-up is accepted: after exploration reward reads return a current payload, the shared `PlayerDashboardShellState` refreshes the existing dashboard page context through `get_player_dashboard_page_context(p_hero_id)` with active hero/server stale guards. XP/Character Points/resources/health topbar state must continue to come from this shared page-context source; do not add direct `hero` reads, local XP calculation or reward inference in Angular. User smoke confirmed the XP topbar updates correctly after reward settlement.
- UI-PERF-ENTRY-1 is accepted with pending user smoke: root landing initial browser bundle is down from 1.09 MB / 231.99 kB estimated transfer to 706.85 kB / 139.50 kB, with budget overage reduced from +541.68 kB to +156.84 kB. Entry/game shell split, lazy route modules/guards, deferred animations/toast and removal of root Auth/Supabase initialization keep `/` lighter; auth initialization now occurs through auth-required guards/routes/services. Remaining optimization candidates: global SCSS and the PrimeNG Aura preset path.
- UI-PVP-1 is accepted/completed after placement cleanup: `/game/vicinity` is the PvP Vicinity target-selection screen with 20-address paginated vicinity rows, generated empty slots, visible target overlay RPC data, selected target panel, `Szpieguj` / `Atak` / disabled `Oblężenie`, protected danger/shield state, self success state, `Moja okolica`, relocation confirmation for empty addresses, daily attacks from RPC and own-estate attack/siege protection from `get_hero_estate_runtime_state`. The route page stays thin; child components live under `src/app/game/components/vicinity`, Vicinity state/helpers under `src/app/game/features/vicinity`, and estate relocation workflow under `src/app/game/workflows/estate-relocation`. Provisional PvP result screens/routes were removed; PvP combat/result/report/ranking flows remain separate follow-ups. Manual smoke remains user-side.
- UI-PVP-2 is accepted/completed as already covered by the accepted UI-PVP-1 implementation: the selected target side panel is a dedicated Vicinity child component with prepared fact rows, backend-derived action availability, empty-address relocation confirmation and no defender-private equipment/snapshot display. No extra production code was required for this task.
- UI-PVP-3 is accepted/completed: `/game/vicinity` now shows an active PvP travel/timer panel from `get_active_pvp_action_offer(p_hero_id)` for attack and spy, reusing `PendingTimerOracle` / `pendingTimerDisplay(...)` with no direct `pvp_actions`, no `metadata_json` parsing, no local timer engine and no combat/result UI. Attack manual-window handoff is covered by UI-PVP-5; spy/result/report handling remains separate.
- UI-PVP-4 is accepted/completed as a static preflight: the generated RPC/types and existing combat module support PvP attack handoff through source `{ sourceEntityType: 'pvp_action', sourceEntityId: pvpActionId }`, using `get_combat_resolution_preview`, `start_manual_combat_session` and `auto_resolve_combat_session` through the existing `MinigameHost` / `CombatHost` path. No local combat engine, direct `pvp_actions` read or `metadata_json` fallback is needed.
- UI-PVP-5 is accepted/completed: attack manual-window PvP actions now route directly from Vicinity to `/game/combat?sourceEntityType=pvp_action&sourceEntityId=<pvpActionId>` through the existing `CombatPage` / `MinigameHost` / `CombatHost` flow. Vicinity keeps only the travel oracle and backend refresh, spy does not enter combat, and Combat owns the manual/auto decision with participants, one-row `Walcz ręcznie` / `Rozstrzygnij auto` actions and a compact decision deadline from the active PvP offer. PvP result/report flow remains a later task. Follow-up: align the large Combat stage/surface wrapper with the accepted global transparent card/surface treatment without local component SCSS.
- UI-PVP-6 is accepted/completed: PvP combat completion now finalizes to backend `game_report_id`, renders an Exploration-style completed report handoff on Combat with `Otwórz raport`, `Kopiuj link publiczny` and compact `Zdobycze`, and uses only safe report detail `reward_section_json` / `item_references_json` for XP, PvP resource transfer and item references. Character Points are not shown as player-facing PvP combat rewards. Active PvP return-to-estate remains a separate backend-offer panel, normal Vicinity target selection is hidden while a blocking PvP action/return is active, notification report routing is explicit allowlist-based, and old PvP result screens/routes/direct table reads/metadata parsing remain absent. Manual smoke remains user-side.
- UI-PVP-7 is accepted/completed: spy now stays in Vicinity only while travelling, using backend `get_active_pvp_action_offer(p_hero_id)` for a compact `GameBar` progress panel with target/address and arrival timing, then settles through `settle_due_pvp_spy_action`, creates a report through `create_pvp_spy_game_report`, and redirects to `/game/reports/:reportId`. `pvp_spy` reports render from safe `spy_section_json` without combat/trial outcome requirements, use the shared outcome/report frame with EquipmentPreview, compact stat/resource display, dashboard-style stat tones and fixed A-E building district columns, active spy and spy reports use the spy route background, and returning spy phases are treated as DB/RPC blockers. No old spy result routes/pages, direct table reads or raw metadata parsing were restored. Manual smoke remains user-side.
- UI-SHELL-27 is accepted after centering check: `mg-container` already centers constrained content with `margin: 0 auto`, and the shell content rhythm pass adds only `$shell-container-max-width` scoped to `.mg-game-shell__main > .mg-container`; existing `max-w-none` still overrides it for admin/wide routes. No feature page templates, TS, DB/RPC, generated types or local component SCSS were touched. Manual visual smoke remains user-side.
- UI-SHELL-28 is accepted: player/staff notification controls remain in the right topbar zone as separate domain components, a shared topbar dropdown coordinator prevents overlapping open dropdowns, outside click closes the open dropdown, and topbar preview rows are flatter and less noisy without technical category/type metadata. `.dropdown-anchor` no longer uses `!important`, row padding uses spacing tokens, and the removed highlight class is covered by visible read/unread badges. No DB/RPC/generated-type changes or broad notification architecture rewrite were added. Manual smoke remains user-side.
- UI-SHELL-29 responsive pass is deferred / not implemented: the responsive-only template changes were reverted, restoring the last accepted shell/topbar baseline. Responsive smoke and any shell/topbar/sidebar responsive fixes remain for a later dedicated pass.
- UI-SHELL-30 through UI-SHELL-32 complete the shell foundation section: the prototype comparison found no runtime fix needed, UI-SHELL-31 removed the empty `game-sidebar.scss` stylesheet hook, and UI-SHELL-32 added the decision-ready report at `docs/ui-ux/ui-shell-32-foundation-acceptance-report.md`. UI-DASHBOARD-02 is accepted as the desktop-first dashboard frame, UI-DASHBOARD-03 is accepted as the desktop-first hero banner identity/context pass, UI-DASHBOARD-04 is accepted as a small real-route hero action strip, UI-DASHBOARD-05 is accepted as the Base Stats card plus dashboard component/mapper/text-color cleanup slice, UI-DASHBOARD-06 is accepted as the Derived Stats row-list polish slice, UI-DASHBOARD-08 is accepted as a reusable real-data paperdoll equipment preview, UI-ITEMS-4 is accepted/completed for Armory shared `EquipmentPreview` paperdoll selection and bulk unequip, and the Health State frontend integration/shared `ActiveHeroVitalsState` cleanup is accepted for dashboard/topbar DB-owned Health and XP display. UI-SHELL-29 responsive work, dashboard mobile/tablet responsiveness and paperdoll responsive geometry/breakpoint polish remain deferred.
- Epic S item/equipment/admin alignment is accepted through S26. The Armory visual cleanup follow-up is accepted: the right-side visible standy area now uses full-width elevated stand cards, clearer full-width headers, inline PrimeNG Inplace rename, icon-only rename/cancel actions, dark input styling, reusable global `radius-circle` / `square-9` badge utilities and the current frozen paperdoll/equipment preview layout was left unchanged. Future accepted Codex implementation tasks should update status docs automatically at acceptance unless the user says otherwise.
- UI-ITEMS-3 is accepted for core shared item popover behavior: item popovers on item icons/cards work, item detail display is usable, icons are visible, and stat/bonus/requirement/value behavior is acceptable for this task. Known follow-up: `EquipmentPreview` / paperdoll responsive geometry remains imperfect across breakpoints, including possible boots/greaves ordering and slot positioning issues; this is a later paperdoll breakpoint refactor, not a blocker for UI-ITEMS-3. Broader manual breakpoint smoke remains tester-side/future.
- UI-ITEMS-4 is accepted/completed for Armory shared `EquipmentPreview` reuse, paperdoll equipped-item selection and canonical bulk unequip through `bulk_unequip_hero_items(...)`. Known follow-up: paperdoll responsive geometry/breakpoint polish remains a later dedicated pass, not a blocker; broader manual visual smoke remains user/tester-side pending.
- UI-ITEMS-5 is accepted for the split Armory visual/header slice only: top header title/copy/metrics direction is accepted, saved loadouts reuse shared `HeroLoadoutPresetsState`, and `mg-section__title` no longer has default gradient styling. This does not complete the original broad filtering/list-visibility scope; split filtering/visibility/pagination/locked-status work remains pending. Armory page split/cleanup is a future refactor, and `EquipmentPreview` geometry/popover behavior should not be churned here. Manual visual smoke remains user-side pending.
- UI-ITEMS-6 is accepted/completed for the `Visible armory` panel header slice: the panel header is reduced to a single `Inventory` heading, while the main Armory header owns page-level metrics and the existing capacity mini-summary cards remain below. Manual visual smoke remains user-side pending. Armory page split/refactor and later item/stand/filter tasks remain separate follow-ups.
- UI-ITEMS-7 is accepted/completed: the redundant Inventory mini-summary cards (`Owned items`, `Visible capacity`, `Hidden`) were removed because these metrics are owned by the main Armory header. No replacement summary row was added, and user-side visual smoke passed. Armory page split/refactor and later item/stand/filter tasks remain separate follow-ups.
- Armory visibility/count hot-fix is accepted: the main Armory header keeps the four-metric layout and now treats `visibilityLimit` as the visible unequipped/inventory capacity, computes displayed inventory count from `totalOwnedItemCount - equippedItemCount`, marks that inventory count as error when it exceeds the capacity, and does not infer totals from the visible item rows returned by `get_hero_armory_items(...)`.
- UI-ITEMS-8 is accepted/completed: stand section headers now keep the stand number, primary stand label, item-count badge and rename action compact and consistent without changing stand order or rename behavior. Manual visual smoke remains user-side pending for custom persisted stand label, default `Stand N` label, `Unsorted` label, item-count badge and rename open/save/cancel. Armory page split/refactor and later item/stand/filter tasks remain separate follow-ups.
- UI-ITEMS-9 is accepted/completed: empty stand bodies now render as a simple intentional empty state with an icon and `Empty stand` only, without helper copy, replacement summary rows or behavior changes. Armory page split/refactor and later item/stand/filter tasks remain separate follow-ups.
- UI-ITEMS-10 visual checkpoint is accepted/completed: visible inventory item cards now use the accepted compact prototype direction with real item icons, item name, classification metadata, drachma value, visible equip/vendor actions and click-to-select card state. The grid/card direction is frozen for this task; do not churn EquipmentPreview, paperdoll, popover, stand headers, empty stands, TS services, DB/RPC or generated types from this acceptance update.
- UI-ITEMS-11 is accepted/completed: visible Inventory item cards show lifecycle/guild badges only for non-default states, keeping `Active` and `Owned private item` hidden so default cards stay compact. Follow-up capacity semantics are also implemented: currently equipped items stay in Equipment Preview / Current loadout, are filtered out of visible Inventory stands, and `Armory capacity` uses stored visible item count so equipped items alone do not create confusing `39 / 30`-style overages. The post-card-cleanup TS pass is accepted: removed dead checkbox/select/move form/control code and kept bulk selection signal-based. Grid, card layout, popover, DB/RPC and generated types remain unchanged. Full manual inventory smoke remains user-side/pending.
- UI-ITEMS-12 is accepted/completed: Armory bulk actions now use a compact reusable toolbar component above and below the visible stand list, showing `N selected · X drachma`, preserving bulk equip behavior and adding confirmed bulk vendor sell through `bulk_vendor_scrap_hero_items(...)` with project-styled PrimeNG confirmation and toast feedback. Accepted Inventory card/grid layout, EquipmentPreview, popovers, stand headers and empty states remain unchanged. Manual smoke remains user-side/pending.
- UI-ITEMS-14 is accepted/completed: Armory Inventory now has a wide search/filter bar with text search over visible item-card text, one `Pierścień` slot option for both ring slots, `Druga ręka` matching shields/off-hand-capable items, player-facing availability filters, top and bottom bulk toolbars, exact normalized match highlighting and no hidden `none`/system-token matches. Bonus search remains a follow-up requiring list-facing DB/RPC/read-model support in `get_hero_armory_items` / `ArmoryItemSummary`. Next larger Armory touch should continue reducing `ArmoryInventorySection` and separating inventory state/rendering, but this is not a blocker for UI-ITEMS-14.
- UI-ITEMS-17 is accepted/completed: the Armory filter bar now includes a stand dropdown sourced from current shelf data. Empty selected stands render as ordinary empty stand sections, global no-result state uses rendered shelf state, and the duplicated filter-bar match-count badge is removed. Follow-ups remain non-blocking: bonus search needs list-facing DB/RPC/read-model support, `ArmoryInventorySection` can be reduced further on a later Armory touch, filter controls may move into a `FormGroup` if the bar grows, and PrimeNG wrapper styling can be revisited in a later vendor-wrapper cleanup.
- UI-ITEMS-18 is accepted/completed: the Armory summary card now shows `Armory capacity` as the visibility limit only, `Total items` as stored inventory items excluding equipped items, and keeps `Equipped items` / `Saved loadouts` unchanged. The redundant `Hidden by capacity` row and explanatory capacity copy were removed; visibility logic, filters, stands, pagination, DB/RPC contracts, generated types and specs were not changed.
- UI-ITEMS-MOVE-1 is accepted/completed: Armory `Move selected` lives in the selected-items toolbar beside `Equip selected` and `Sell selected`, uses the canonical single-item `ArmoryShelfState.moveItemToShelf(...)` / `PlayerArmory.moveItemToShelf(...)` / `move_hero_armory_item_to_shelf` path, and selected item cards use global `.mg-card--selected`. UI-ITEMS-MOVE-2 is also accepted/completed: eligible stored item cards can be dragged by grabbing the card surface and dropped onto another rendered stand/shelf, including empty stands, through Angular CDK drag/drop while reusing the same canonical single-item move path; same-stand drops no-op, ineligible/guild/locked/non-active items are not draggable, and the toolbar `Move selected` fallback remains. UI-ITEMS-MOVE-3 is accepted/completed: toolbar bulk move now supports multiple eligible selected stored items from one or many stands through `ArmoryShelfState.bulkMoveItemsToShelf(...)` / `PlayerArmory.bulkMoveItemsToShelf(...)` / `bulk_move_hero_armory_items_to_shelf`, without looping the old single-item RPC; mapper and feedback formatting are outside service/page code, skipped/no-op outcomes are not fatal, toast copy is player-facing, and no local SCSS/direct DB writes/generated-type edits were added. UI-ITEMS-MOVE-4 is accepted/completed: dragging one selected eligible item now moves the selected movable group through the same canonical bulk path without looping the single-item RPC, while unselected drag still uses single-item move; custom CDK preview/placeholder, active group hiding, Escape cancel guarding, equipment/Armory refresh after equipment mutations, extracted equipment toast formatting and final Armory spacing cleanup are accepted.
- UI-ITEMS-PRESETS-1 is accepted/completed: the Armory left column now keeps Equipment Preview first and renders a compact `Saved presets` panel below it, with stable row layout, inline rename, `Save preset`, saved-only `Apply`, saved-only danger `Clear`, and name hover/focus preview instead of a dedicated Preview button. The preview uses existing popover styling, filters item rows before applying separators, suppresses available-item status helper text, and keeps an explicitly named slot-icon fallback because the read model does not expose item classification fields. No preset RPC, DB/generated type, inventory, Equipment Preview or move/drag/drop behavior changes were added.
- UI-ITEMS-19 is deferred / potentially cancelled: current user smoke considers visible Inventory usable enough, so large-list pagination/scroll work should not be added speculatively; reopen only if representative large-list testing shows a real usability issue and then follow UI-CORE-14. UI-ITEMS-20 is accepted/completed: final Armory visual consolidation removed the obsolete nested Inventory heading wrapper, kept existing heading rhythm and spacing, and confirmed no prototype/debug/transitional markup leaks in touched Armory paths.
- UI-EXPLORATION-0 is accepted/completed as the Exploration prototype/source preflight: `mythborne_exploration_flow_v_2.html`, the prototype-production mapping, current Exploration page/status/state files, `getActiveDifficultyTiers()` and `getHeroExplorationState(...)` were mapped, with richer prototype anchors deferred to later UI-EXPLORATION tasks. UI-EXPLORATION-1 is accepted/completed: `/game/exploration` now has the accepted header/top summary surface with DB/read-model-backed difficulty, route state, remaining Trials and active effect values. UI-EXPLORATION-2 is accepted/completed: no-exploration now flows directly from the header/top summary to Difficulty with no extra status card, active exploration gets an active-only compact current-route details surface, header route state uses `runtimeStatusLabel()` as the single label source, raw node ids are not used as player-facing fallbacks, and the lower legacy `ExplorationStatusSection` remains after Difficulty for timer/directions/results/rewards/diagnostics rather than being used as the top compact status card. UI-EXPLORATION-3 is accepted/completed: active movement step progress is now a dedicated inline panel in `exploration-page.html`, gated by `page.activeStep()` after header/feedback and before Difficulty, using existing backend-owned timestamp/readiness display signals and keeping `Check result` visible only when the step is ready or resolving. The duplicate active-step card and stale no-exploration start branch were removed from the lower legacy `ExplorationStatusSection`; remaining raw-ish legacy runtime chips are deferred to later runtime/UI-EXPLORATION-14 cleanup. UI-EXPLORATION-4 is accepted/completed: Difficulty cards now use local selected-state handling, existing production selected-state classes and DB/read-model-backed availability badges without adding start CTA, timer, directions, results, rewards, DB/RPC/schema/generated types or local SCSS. UI-EXPLORATION-5 is accepted/completed: Difficulty preview cards and selected Trial detail rows use the canonical `get_hero_exploration_difficulty_card_previews(...)` read model, render DB-provided Step time, Trial opportunity, Approx. manifestation, Approx. auto result and Reward items through focused preview components and shared `GameBar type="chance"` bars, enforce 9 selected stat rows, and keep Trial power/Tested stat/source rows out of player-facing detail. UI-EXPLORATION-6 is accepted/completed: available Difficulty cards are whole-card selectable, selected cards own the stable `Start exploration` / `Continue adventure` CTA, status badges and the separate current-route/status card are removed from the entry screen, and the top summary uses the selected preview read-model Approx. auto result instead of Route state. UI-EXPLORATION-7 is accepted/completed: `/game/exploration` now switches between the accepted Difficulty entry screen and a distinct minimal runtime shell driven by existing read-model/page state, without rendering pending timer, result, direction, reward, challenge or diagnostics screens ahead of later runtime tasks. UI-EXPLORATION-8 is accepted/completed: active movement steps now render as the main runtime pending-step screen with backend-derived progress, remaining/ready state, friendly started/ready metadata and a `Check result` action only when existing readiness allows it. UI-EXPLORATION-9 is accepted/completed: checked movement steps now render a scoped `Step report` outcome surface with existing backend/read-model-backed title, description and flavor helpers plus short challenge, reward/report and next-action handoff cards; sandbox diagnostics remain gated. UI-EXPLORATION-10 is functionally accepted/completed for movement-option integration: direction choices render from backend-owned `movementOptions[]`, edge choices start movement with `edgeId` plus `stepKind`, optional backtrack starts with `edgeId: null` plus `stepKind: 'backtrack'`, and active step/challenge state blocks movement. Manual smoke remains pending for 1/2/3 edge options, optional backtrack and active step/challenge blocking; visual direction-board prototype polish is a separate follow-up task.
- Epic T Guild Foundation is accepted through T24. T0 verified current generated guild DB/RPC types; T1 added guild domain models, RPC type aliases and mapper tests; T2 added the canonical guild read service and current guild state; T3 added guild discovery/search service, state, generated RPC aliases and mapper coverage; T4 added the create-guild service/action and form-ready state over canonical RPC/config contracts; T5 added the guild invite RPC service/state and focused invite mappers over canonical invite contracts; T6 added request-to-join RPC service/state and focused join-request mappers over canonical join request contracts; T7 added guild member list/action service, state and focused member mappers over canonical member RPC contracts; T8 added guild leave/disband lifecycle service, state and focused lifecycle mappers over canonical lifecycle RPC contracts; T9 added emergency leader election read service/state and focused election mappers over canonical read RPC contracts; T10 added emergency leader election action service/state over canonical RPC contracts; T11 added guild armory read/action models, split read/action mappers and services over canonical guild armory RPC contracts; T12 added a read-only guild armory UI section/state for current items, available/borrowed state, borrower display, current loans and DB-backed capacity; T13 added guild armory deposit, owner withdraw and leader/officer remove UI actions through canonical `PlayerGuildArmoryActions`, with equipped-item deposit blocked before RPC and header refresh reloading both read and deposit contexts; T14 added guild armory borrow and return UI actions through canonical `PlayerGuildArmoryActions`, with visibility driven by DB-backed `canBorrow`/`canReturn` flags and no frontend ownership-transfer/trade/auction/vendor/equipment behavior; T15 added guild armory force-return UI actions through canonical `PlayerGuildArmoryActions.forceReturnGuildArmoryLoanForActiveHero(...)`, with visibility driven by DB-backed `canForceReturn`, no direct table access, no local ownership-transfer logic and no borrower equipment simulation; T16 added per-member guild armory access status and leader/officer block/unblock UI through `get_hero_guild_members(...)` and canonical `PlayerGuildArmoryActions.setGuildArmoryMemberAccessForActiveHero(...)`, with ToastService feedback and no local access-state fallback; T17 aligned player Armory item UI with guild armory usage through canonical guild armory reads and hides private item actions for deposited/borrowed guild items; T18 added a concise guild support placeholder that explains future siege and Argonautics group support as guild-membership-bound without fake actions or diplomacy UI; T19 added a read-only admin guild config summary to config definitions using canonical `PlayerGuild.getGuildConfigSummary()`, with unlimited armory capacity rendered clearly and no config mutation/direct DB access; T20 added `/game/guild`, a sidebar `Guild` entry and a thin page over `CurrentGuildState` that shows no-guild entry areas, in-guild overview/member/election summaries and the existing guild armory section without fake siege/Argonautics actions; T21 replaced the no-guild entry placeholders with real create/search/request/cancel/accept/reject UI over existing canonical guild states/services, with search read errors inline and transient action feedback through toasts; T22 added in-guild membership management UI for member list, invite create/cancel, incoming join request accept/reject and outgoing request cancel flows over existing canonical guild states/services, with capability-flagged actions, inline read errors and toast action feedback; T23 added kick/promote/demote/leave/disband UI over existing canonical member and lifecycle states/services, with role/lifecycle UI guards, required disband reason validation, toast action feedback and current guild/member-list refresh after success; T24 replaced the emergency election placeholder with real summary/candidate/start/nominate/start-voting/vote/finalize UI over `GuildEmergencyElectionState`, DB-backed capability flags and canonical election services/actions, without local quorum/result inference. Manual smoke remains pending for `/admin/config-definitions`, `/game/guild` create/search/request/cancel/invite flows, `/game/guild` in-guild invite/request/member-management flows, T23 role/lifecycle flows and T24 emergency election flows; T23 smoke is data-blocked because the current server has only one character/guild member, and T24 smoke is data-dependent on an eligible emergency election scenario.
- Epic U Luck Foundation is accepted through U13 cleanup. U0 confirmed generated Luck Foundation contracts without code changes; U1 added Luck domain models, generated RPC aliases and mappers; U2 added the canonical Luck RNG surface registry read service; U3 added Trial Power read/preview integration over DB RPCs; U4 aligned exploration RNG read state so `nothing` is the canonical fallback outcome and trial/encounter rolls preserve DB-returned chance/roll/Luck metadata; U5 added nested Trial Manifestation read-state on challenge attempts; U6 added nested Challenge Auto-resolve read-state while keeping combat challenges on the live-combat/manual boundary; U7 added DB-owned combat Luck RNG read-state on live timing manifests, split combat live mappers by responsibility and preserved `submit_combat_player_action(...)` as timing-input-only with no client-side damage/outcome/equipment/stat/Luck formula authority; U8 switched Exploration Lab generated item preview to DB-owned `preview_reward_generated_item_luck(...)`, preserves bucket/value/quality/base/prefix/suffix/budget/Luck breakdown and keeps one-roll copy clear that Luck does not guarantee a better single item; U9 switched reward profile previews to DB-owned `preview_reward_profile_luck(...)`, preserving reward range/Luck policy/formula context without Angular-side reward RNG or amount formulas; U10 added Exploration Lab Luck surface registry/readability over `get_luck_lab_preview_contracts()` and DB-returned Luck influence/Trial Power/formula keys in chance previews without becoming a standalone Luck Lab; U11 made Luck Foundation formula targets readable in existing formula governance/admin surfaces, with metadata-first help, raw Luck vs Luck influence labels, assigned/variable-compatible tester target selection and successful smoke for `Default trial manifestation chance` after migrator fixed the live `trial_manifestation_chance` scope mismatch; U12 player-facing Luck copy direction was cancelled and accepted as cleanup: Exploration no longer shows explicit counterfactual Luck-impact copy, `Luck:` DB explanations, or auto-resolve technical Luck/Trial Power/cap facts in player-facing challenge/timing labels, with challenge/live-combat helpers extracted out of state files; U13 removed stale local item-generation/Luck RNG fallback paths, deleted the local generated-item demo surface, and routes generated PvE opponent equipment through the DB-owned opponent combatant snapshot contract without restoring local item/Luck/affix RNG or using preview RPCs as runtime. U8 manual smoke remains pending for the Exploration Lab generated item preview; U9 manual smoke remains pending for Exploration Lab and Reward Profiles reward-profile preview surfaces; U10 manual smoke remains pending for Exploration Lab Luck surface registry and chance preview readability; U12 manual smoke remains pending for `/game/exploration` active challenge/live combat rendering without player-facing Luck copy; U13 manual smoke remains pending representative generated-opponent data. Follow-ups: `ExplorationLabPageState` is a split candidate on the next larger Exploration Lab task; when representative generated opponent equipment data exists, smoke the generated PvE opponent path end-to-end and confirm result/report attack source component refs render correctly.
- Epic V Luck Lab is complete and accepted through V15. V0 confirmed current generated DB types included the initial Luck Lab preview contracts, and V10 now consumes the regenerated DB-owned drop-distribution simulation contract. V1 added Luck Lab domain models and mapper coverage for input/result, Luck influence, Trial Power, chance previews, combat preview, reward range preview, generated item preview, comparison rows, explanation rows and explicit unsupported drop distribution, without editing generated types or adding local formula/drop simulation authority. Generated item Luck previews now model optional prefix/suffix as nullable affix objects, normalizing absent affixes to `null` instead of semantic empty strings/zeros. V2 added reusable `LuckLabPreviews` and `LuckLabState` over DB/RPC preview contracts with signal inputs, debounced reloads, stale-response guards, section-specific loading/error/result patching and no duplicated Trial Power RPC in aggregate preview. V3 added `/admin/luck-lab`, dashboard/navigation entry, a thin page plus page state with shared Luck/tested-stat sliders and DB-backed difficulty/district/stat/trial selectors over existing exploration definition state. V4 added the Trial Power panel with tested stat, raw Luck, Luck influence, final Trial Power, equation summary, DB formula keys/expressions, explanation and DB-backed comparison rows; form defaults now use `DEFAULT_LUCK_LAB_INPUT`. V5 added the Trial chances panel, separating Trial opportunity from Trial manifestation, showing DB/RPC chance previews with percent units, DB formula/explanation output, selected difficulty/district context, DB-backed trial label/key as the primary trial context and Luck 0/current/high comparison rows through feature-local `LuckLabComparisonState`. V6 added the Auto-resolve panel with DB-owned binary success chance, tested stat, Luck influence, Trial Power, raw chance, cap, difficulty multiplier, manual chance reference and Luck 0/current/high comparison rows through `LuckLabPreviews.previewChallengeAutoResolve(...)`; selected trial/minigame context is marked as not consumed by the current RPC. V7 added the Encounter fallback panel with DB-owned non-trial encounter chance, base/raw/final/cap breakdown, Luck 0/current/high comparison rows through `LuckLabPreviews.previewNonTrialEncounter(...)`, explicit `nothing` deterministic-fallback copy and resource events kept as encounter subtype. V8 added the Combat RNG panel over DB-owned `previewCombat`, showing hit/evasion/critical/critical multiplier/final damage/initiative plus Luck preset comparison rows, with combat-specific section state, no local `formulasJson` compatibility parser, formula target keys shown as metadata only and missing stable combat formula labels / critical multiplier target tracked as a DB metadata gap. V9 added the single-roll generated item panel over DB-owned `preview_reward_generated_item_luck(...)`, forcing `p_preview_count = 1`, showing bucket/max quality context, rolled bucket, quality, base item, nullable prefix/suffix, final value and budget breakdown, and keeping reward profile and generated item loading/error sections separate so hidden reward profile failures do not poison the panel or page shell. V10 added the Drop distribution panel over DB-owned `preview_reward_generated_item_distribution_luck(...)`, showing roll count, average/median/min/max value, bucket and quality distributions for current Luck and Luck 0 comparison, prefix/suffix hit rates, high-value/outstanding rates and average delta, with scoped `dropDistribution` loading/error and copy that Angular only renders DB-returned summary rows. V10 cleanup reused the unsupported summary helper for no-row fallback, removed unused `prefixRows` / `suffixRows`, and extracted shared bucket/quality option labels. V11 added shared Luck comparison presets for `Luck 0`, low, medium, high and current slider values, reused across Trial Power, trial chance, auto-resolve, encounter and combat comparisons, plus a section-local drop-distribution comparison state using the same preset helper and DB-owned distribution preview RPC. Preset labels are UI baselines because no DB/config preset source is exposed; all comparison values remain DB/RPC-owned. V12 added DB-backed human-readable explanation lists over `get_luck_lab_preview_contracts()` / `LuckLabPreviewResult.explanationRows`, using DB labels/descriptions/helper text plus returned preview explanations/formula metadata, removing local explanation fallback prose and render-time throws, and labeling summary/default/null values so cards do not show standalone `DB default` or `N/A`. V13 added formula governance navigation from Luck Lab explanation rows to the existing generic `/admin/formulas` route, showing returned/known formula keys or target keys as secondary metadata where target-specific deep links are not available, without adding a formula editor or fake deep links. V14 added per-section request tokens, independent fast-section vs drop-distribution scheduling, pending fast-section batching, longer debounce for DB-owned drop-distribution previews and comparison rows, current-input preservation when older still-valid section results complete, and extracted section request/patch orchestration to `luck-lab-section-requests.ts`. V15 added `docs/luck-lab-final-integration-report.md` as the final Epic V report covering implemented panels, DB/RPC contracts, Luck Foundation vs Luck Lab boundaries, limitations, follow-ups, verification reference and pending manual smoke. `selectedCombatProfileKey` is future input only because current combat preview RPC does not consume it. Manual smoke remains pending for `/admin/luck-lab`, including Luck/bucket/max-quality distribution refresh, preset rows, DB-backed explanation rows, formula governance links, no standalone default/null card values, no NG0955 warnings, slider responsiveness and stale UI behavior. Follow-up for later V/UI tasks: `luck_influence` comparison rows must not be presented as normal gain/loss.
- Epic W Exploration Core is accepted through W13 as of 2026-05-11. W0 confirmed generated DB types expose the Exploration Core readiness, selection diagnostic, reward diagnostic, timer duration and sandbox helper contracts without frontend/generated-type edits. W1 added typed Trial/Encounter readiness, readiness reason metadata and step selection diagnostic domain models, generated RPC aliases and focused mappers/specs over DB/RPC rows. Selected Trial/Encounter diagnostics now require complete id+key identity; partial identities map to `null`. W2 aligned step resolution with canonical Trial/Encounter/Nothing outcomes, preserves raw DB outcome as diagnostics, maps DB-owned selected definition / selection diagnostic metadata, and lets `/game/exploration` distinguish Combat/Resource/Buff/Debuff Encounters only when DB metadata provides `encounterKind`. W3 added sandbox-gated `/game/exploration` selection diagnostics for sandbox-access contexts, with selected/final outcome, selected/skipped definition context, readiness reason labels and collapsed raw selection payload while hiding diagnostics on standard/live servers. W4 integrated DB/RPC-owned Trial readiness into the existing `/admin/exploration-trials` selector/list/detail surfaces via `get_trial_definition_readiness` and the existing W1 mapper, without Angular-side readiness recomputation or blocking incomplete Trial edits. W5 integrated DB/RPC-owned Encounter readiness into the existing `/admin/exploration-encounters` selector/list/detail surfaces via `get_encounter_definition_readiness` and the W1 readiness mapper, sharing Trial/Encounter readiness UI helpers and keeping incomplete Encounter config editable/saveable. W6 added `/admin/exploration-debug` step duration config visibility over `get_exploration_step_duration_seconds(...)`, exact-key `exploration_step_duration_seconds` config explainability diagnostics and DB-duration-preserving warnings when explainability is missing/failing, without player timer/runtime changes. W7 surfaced the active step timer from loaded debug state and lets sandbox testers prefill the existing skip form before calling `skip_hero_exploration_step_timer(...)`, preserving scope/stale guards, debug refresh and normal player timer behavior. W8 shows the current loaded Trial attempt count in `/admin/exploration-debug`, uses Polish copy for adding Trial attempts, and still mutates only through `add_hero_remaining_actions(...)` with the existing refresh/stale-guard path. W9 cleans up `/game/exploration` Trial/Encounter action UI so Combat Trial/Encounter stays on the live-combat flow, Trial manual/auto actions appear only when DB state supports them, non-combat Encounter active states show blockers instead of Trial resolve buttons, and Buff/Debuff outcomes describe DB-applied effect context when refreshed state returns `activeEffect`. W10 hardens `/game/exploration` reward/drop display over durable `reward_grants`, `reward_grant_entries` and persisted `items`, with Polish-facing copy, visible XP/Character Points entries, generated-item rows hidden when `itemId` is absent but surfaced through DB diagnostics, and no reward generation/runtime changes. W11 is accepted: the stale current-event/latest-challenge reward fallback and fallback scan of `hero_exploration_challenge_attempts` were removed; challenge rewards now read only exact `getChallengeReward({ challengeAttemptId })`; direct Resource Encounter rewards read exact `getStepReward({ stepId })`; Trial manifestation failure is no longer presented as ordinary Nothing; empty/null canonical reward read models show explicit DB-read-model unavailability; resource reward labels no longer duplicate resource type text; sandbox backend-shape diagnostics remain gated. W11 also consumes the regenerated contextual report read model in private/public report detail: `trial_section_json`, `encounter_section_json`, `combat_section_json`, `reward_section_json`, `effect_section_json`, `related_reports_json` and `source_label`. The report renderer shows DB-owned Trial/Encounter story sections before reward/effect/combat/related report sections, hides the old detail-page missing-metadata warning, and does not throw on absent combat attack source labels. Angular still must not infer rewards/effects/reports from Armory, selection diagnostics, stale latest challenge or raw metadata. W12 accepted: `/admin/exploration-debug` shows the global 10-row smoke readiness matrix over existing Trial/Encounter admin read models, with ready/missing/incomplete status for required definitions, reward paths and active effect paths; screenshot smoke confirmed ready rows, while missing/incomplete live states are covered by focused mapper specs. W13 accepted: `/game/exploration` hides stale challenge completion, reward and completed combat state across the next movement/result context, keeps exact reward reads on `getStepReward(stepId)` / `getChallengeReward(challengeAttemptId)`, uses Polish-facing blocker/error/success feedback in touched challenge/movement flow paths, and still reads active effect label/summary from DB-returned `state.activeEffect.metadataJson` without local resolver/fallback authority. W14 is deferred for a later Exploration Core completion/handoff report after representative real-session smoke. Manual full-loop smoke remains pending on real data/session. Manual smoke for W1 is N/A; W2 user-side manual smoke remains pending for Trial, Combat Encounter, Resource Encounter, Buff/Debuff Encounter and Nothing outcomes; W3 user-side manual smoke remains pending for sandbox tester/admin visibility, standard/live hiding and collapsed raw payload behavior; W4 user-side manual smoke remains pending for ready/incomplete/inactive Trial display, readable blocking reasons and saving incomplete Trials; W5 user-side manual smoke remains pending for ready/incomplete/inactive Encounter display, readable Combat/Resource/Buff/Debuff blocking reasons and saving incomplete Encounters; W6 user-side manual smoke remains pending for effective duration rows, inferred/base diagnostic, missing/failing config warning and player timer behavior staying DB-owned; W7 user-side manual smoke remains pending for sandbox/tester active-step load, use-active-step prefill, skip, refreshed state and live/normal access hiding; W8 user-side manual smoke remains pending for sandbox/tester current Trial count, add attempts, refreshed count and live/normal access hiding; W9 user-side manual smoke remains pending for Trial manual/auto, Combat Trial/Encounter live combat, Resource immediate outcome, Buff/Debuff effect outcome and Nothing outcome; W10 user-side manual smoke remains pending for XP/PP reward, item drop, legal no-item drop, DB failure reason and refresh safety; W11-W13 accepted with final live-loop manual smoke still pending where representative data/session is required.
- Epic X Onboarding / Start Flow is accepted through X6 as of 2026-05-11. X1 added the typed start-flow integration layer over canonical `get_start_flow_server_availability`, `get_start_flow_origin_options` and `create_hero_start_flow`, replaced the legacy Angular direct-write hero bootstrap with the atomic DB/RPC creation workflow, reloads `ActiveHero` through selected server -> active hero after creation, blocks unsupported DB `route_next_action` values, and shows explicit origin option loading/error/empty states. X2 added `/auth/server-entry` for server picker and entry routing over the same DB availability read model: standard servers route to hero creation, dashboard or visible blockers from DB `next_action` / `can_enter_game` / `can_create_hero` / `block_reason`; sandbox multi-hero selection is gated by DB-allowed hero selection/entry and DB-returned hero options; dashboard entry requires active hero context. X3 updated hero creation so hero name + origin are the only player inputs, origin copy/description/bonus summary come from DB-backed start-flow origin options, one-time origin selection is explicit, creation blockers are Polish/player-readable for duplicate name, district/server full, invalid origin and permission/membership cases, and the creation error mapper reuses `getErrorMessage(...)` while staying feature-local. X4 confirmed fresh creation routes from DB `stat_allocation` to `/hero/attributes`, returning existing heroes route to `/hero/dashboard`, and `/hero/attributes` has a normal dashboard exit link. X5 completed sandbox/test multi-hero switching with DB-returned hero options in a compact `p-select` switcher, DB `defaultHeroId` as the only default authority, guarded `ActiveHero.selectHero(...)` switching, active-hero handoff to the newly created `result.heroId`, and create-another sandbox hero entry allowed only when selected server availability returns `canCreateHero` without a blocker. X6 hardened the final integration by clearing stale creation availability on user-context changes, keeping availability loads token-guarded and blocking submit until selected server availability is freshly confirmed. DB/RPC owns starting Character Points, CP ledger, random district-A estate, starting stats/resources and route handoff; stat allocation save remains on canonical `save_stat_allocation(...)`. Origin bonus display uses DB `bonus_summary_text`; origin artwork remains the existing static carousel convention derived from `origin_key`, not DB-backed artwork. Manual/browser smoke remains pending.
- ADMIN-FOLLOWUP-1 is accepted: `/admin/config-definitions` now has a governed editable guild config form over `GuildConfigEditorState`, with sequential draft-entry-ready-apply-reload orchestration and explicit mapping from non-prefixed `get_guild_config_summary()` fields to prefixed canonical guild `config_definitions.key` values. Creation cost and `armoryCapacity = 0` / `Unlimited` manual smoke passed per acceptance.
- Completed and confirmed: A1 - regenerate/update Supabase database types.
- Current documentation sync applied: A2 - this TODO and `current-state-summary.md` were updated after A1 confirmation.
- Completed and confirmed: B1 - audit old identity assumptions.
- Completed and confirmed: B2 - standardize active server resolver.
- Completed and confirmed: B3 - standardize active hero resolver.
- Completed and confirmed: B4 - migrate stats/resources/progression to active hero id.
- Completed and confirmed: B5 - migrate estate/building/item/combat reads to active hero id.
- Completed and confirmed: C1 - add role/membership read layer.
- Completed and confirmed: C2 - staff server switcher.
- Completed and confirmed: C3 - membership status UI handling.
- Completed and confirmed: D1 - config definitions read model.
- Completed and confirmed: D2 - config values read model.
- Completed and confirmed: D3 - config change-set list/detail.
- Completed and confirmed: D4 - config edit draft flow.
- Completed and confirmed: D5 - config apply/cancel flow.
- Completed and confirmed: D6 - anti-abuse config admin section.
- Completed and confirmed: E1 - formula targets/formulas read layer.
- Completed and confirmed: E2 - formula assignment viewer.
- Completed and confirmed: E3 - local entity formula assignment support.
- Completed and confirmed: E4 - formula runtime integration cleanup.
- Completed and confirmed: F1 - inspect current bonus template usage.
- Completed and confirmed: F2 - design bonus model legacy retirement plan.
- Completed and confirmed: F3 - canonical bonus domain models and mappers.
- Completed and confirmed: F4 - bonus dictionary/admin read service.
- Completed and confirmed: F5 - bonus template write path migration.
- Completed and confirmed: F6 - entity bonus read model and payload helpers.
- Completed and confirmed: F7 - origin bonus read migration.
- Completed and confirmed: F8 - item generation base type model migration.
- Completed and confirmed: F9 - item generation entity bonuses.
- Completed and confirmed: F10 - building entity bonuses.
- Completed and confirmed: F11 - combat/equipment item bonus inputs.
- Completed and confirmed: F12 - legacy bonus usage cleanup audit.
- Completed and confirmed: G1 - audit dictionary read layer.
- Completed and confirmed: G2 - audit log read layer.
- Completed and confirmed: G3 - audit domain operation helper.
- Completed and confirmed: G4 - audit config governance changes.
- Completed and confirmed: G5 - audit anti-abuse decisions.
- Completed and confirmed: G6 - audit gameplay persistent changes, stat allocation RPC slice.
- Completed and confirmed: H1 - anti-abuse dictionary models, satisfied by UX-I8.
- Completed and confirmed: H2 - anti-abuse dictionary loaders.
- Completed and confirmed: H3 - anti-abuse case read models.
- Completed and confirmed: H4 - server-scoped case list service.
- Completed and confirmed: H5 - case detail aggregation service.
- Completed and confirmed: H6 - player relationship declaration form model.
- Completed and confirmed: H7 - player relationship declaration submission.
- Completed and confirmed: H8 - player declaration list/status read-model service slice.
- Completed and confirmed: H9 - staff relationship declaration review service slice.
- Completed and confirmed: H10 - player abuse report form model.
- Completed and confirmed: H11 - player abuse report submission.
- Completed and confirmed: H12 - player abuse report list/status read-model service slice.
- Completed and confirmed: H13 - staff case list page.
- Completed and confirmed: H14 - staff case detail page.
- Completed and confirmed: H15 - case status transition action.
- Completed and confirmed: H16 - case verdict action.
- Completed and confirmed: H17 - sanction domain models.
- Completed and confirmed: H18 - sanction type-driven form model.
- Completed and confirmed: H19 - sanction creation operation.
- Completed and confirmed: H20 - sanction status update operation.
- Completed and confirmed: H21 - CP penalty view/management.
- Completed and confirmed: H22 - repeat offender/history view.
- Completed and confirmed: I1 - add lifecycle fields to item domain models.
- Completed and confirmed: I2 - filter scrapped items from normal inventory.
- Completed and confirmed: I3 - implement safe scrap behavior skeleton.
- Completed and confirmed: I4 - staff item recovery operation.
- Completed and confirmed: J1 - align trade/auction frontend plan with existing DB/RPC contract.
- Completed and confirmed: J2 - direct trade read models and services.
- Completed and confirmed: J3 - direct trade mutation UI through existing RPCs.
- Completed and confirmed: J4 - auction gameplay UI through existing RPCs.
- Completed and confirmed: J5 - trade/auction transaction item snapshot feature integration.
- Completed and confirmed: J6 - trade and auction audit frontend alignment.
- Completed and confirmed: J7 - vendor scrap/sell for drachmas core service alignment.
- Completed and confirmed: K1 - anti-abuse signal generation contract alignment.
- Completed and confirmed: K2 - anti-abuse signal and case read models.
- Completed and confirmed: K3 - trade/auction signal review integration.
- Completed and confirmed: K4 - identity observation / same-IP-device integration boundary.
- Completed and confirmed: K5 - signal grouping and case explainability UI.
- Completed and confirmed: L1 - DB/types alignment after PvE migrations.
- Completed and confirmed: L2 - exploration domain models and mappers.
- Completed and confirmed: L3 - player exploration start/status screen.
- Completed and confirmed: L4 - graph state read and direction UI.
- Completed and confirmed: L5 - start step timer and progress UI.
- Completed and confirmed: L6 - resolve step result UI.
- Completed and confirmed: L7 - challenge attempt UI: manual, auto and debug paths.
- Completed and confirmed: L8 - reward display and item persistence confirmation.
- Completed and confirmed: L9 - admin exploration debug page.
- Completed and confirmed: L10 - exploration lab / preview / simulation UI.
- Completed and confirmed: L11 - trial definitions admin configurator.
- Completed and confirmed: U0-C1 - frontend role usage audit.
- Completed and confirmed: U0-C2 - staff gameplay access audit.
- Completed and confirmed: U0-C6 - staff/moderation navigation boundaries audit.
- Completed and confirmed: U0-C3 - user/staff management UI audit.
- Completed and confirmed: U0-C4 - moderator scope UI spec.
- Completed and confirmed: U0-I1 - central staff access policy model.
- Completed and confirmed: U0-I2 - staff gameplay boundary implementation.
- Completed and confirmed: U0-I3 - admin route guard and sidebar boundary.
- Completed and confirmed: U0-I4 - admin dashboard cards and tag-link filtering.
- Completed and confirmed: U0-I5 - staff management read models and services.
- Completed and confirmed: U0-I6 - staff management UI foundation.
- Completed and confirmed: U0-I7 - moderator scope assignment UI.
- Completed and confirmed: U0-I8 - moderation actions UI foundation.
- Completed and confirmed: U0-I9 - moderation history target picker and full-history modes.
- Completed and confirmed: UX-I1 - shared metadata display helper.
- Completed and confirmed: UX-I2 - config governance explainability implementation.
- Completed and confirmed: UX-I3 - audit log readability pass.
- Completed and confirmed: UX-I4 - formula impact preview calculators.
- Completed and confirmed: UX-I5 - item generation quality impact preview.
- Completed and confirmed: UX-I6 - building impact calculator.
- Completed and confirmed: UX-I7 - building bonus and requirement explainability.
- Completed and confirmed: UX-I7b - DB-driven central requirement editor for Buildings admin.
- Completed and confirmed: UX-I8 - anti-abuse decision explainability pass.
- Completed and frontend-confirmed: L12 - encounter definitions admin configurator.
- Completed and confirmed: L12b - resource and effect encounter payload configurators.
- Completed and confirmed: L12c - encounter configurator explainability and layout pass.
- Completed and confirmed: L13 - reward profile configurator.
- Completed and confirmed: L11c - trial configurator explainability and layout pass.
- Completed and confirmed: M0 - align generated DB types after Epic M schema foundation.
- Completed and confirmed: M1 - formula random runtime/editor support.
- Completed and confirmed: M2 - combat domain contracts.
- Completed and confirmed: M3 - hero combatant resolver and critical damage debt.
- Completed and confirmed: M4 - opponent definitions and combat dictionaries read layer.
- Completed and confirmed: M5 - opponent combatant/loadout resolver.
- Completed and confirmed: M6 - attack plan builder.
- Completed and confirmed: M7 - initiative and turn order.
- Completed and confirmed: M8 - core combat resolver with slot execution.
- Completed and confirmed: M9 - persist combat result snapshot through canonical RPC.
- Completed and confirmed: M10 - thin sandbox combat caller.
- Completed and confirmed: M11 - combat admin/balance tooling foundation.
- Completed and confirmed: M12 - combat opponent definitions admin configurator.
- Completed and confirmed: N0 - align generated DB types after Epic N DB foundation preflight.
- Completed and confirmed: N1 - terminology cleanup: Health vs Character Points.
- Completed and confirmed: N2 - stat allocation alignment with existing RPC.
- Completed and confirmed: N3 - stat upgrade cost formula usage audit/fix.
- Completed and confirmed: N4 - stat level cap formula usage audit/fix.
- Completed and confirmed: N5 - XP and level display over current DB/RPC contract.
- Completed and confirmed: N6 - XP grant workflow integration boundary.
- Completed and confirmed: N7 - progression ledger and history read models.
- Completed and confirmed: N8 - level-up reward visibility and routing awareness.
- Completed and confirmed: N9 - level-up stat bonus rules and grant display/admin alignment.
- Completed and confirmed: N10 - derived stat resolver cleanup, including critical damage.
- Completed and confirmed: N11 - Character Points display, ledger and penalty sink clarity.
- Completed and confirmed: N12 - Progression admin/formula/configurator explainability.
- Completed and confirmed: N13 - Progression integration smoke and blocker report technical checkpoint.
- Completed and confirmed: O1 - DB/types alignment after estate foundation cleanup.
- Completed and confirmed: O2 - Estate address and capacity read layer.
- Completed and confirmed: O3 - Empty-address relocation flow through the Vicinity picker.
- Completed and conditionally accepted: O4 - Building catalog and estate building read layer, including the formula editor/tester variable-contract follow-up after BUILDING-FORMULA-DB-FIX2/FIX3.
- Completed and confirmed: O5 - Building job read layer and lazy finalization.
- Completed and confirmed: O6 - Start building construction/upgrade flow, settled mansion runtime state and active/completed job smoke.
- Completed and confirmed: O8 - Building configurator edit alignment.
- Completed and confirmed: O9 - Estate vicinity/address browser and relocation picker UI.
- Completed and confirmed: O10 - Player estate overview and building dashboard UI.
- Completed and confirmed: O11 - Estate/building feedback and notification integration.
- Completed and confirmed: O12 - Estate/building integration smoke and blocker report technical checkpoint.
- Completed and confirmed: P1 - Game report domain models and mappers.
- Completed and confirmed: P2 - Private Reports list / Reports tab prototype.
- Completed and confirmed: P3 - Private report detail and mark-read flow.
- Completed and confirmed: P4 - Public report route.
- Completed and confirmed: P5 - Combat report renderer.
- Completed and confirmed: P6 - Combat report creation integration.
- Completed and confirmed: P7 - Reward/drop item reference display.
- Completed and confirmed: P8 - Attach reward drops to reports through approved producer path.
- Completed and confirmed: P9 - Trial and encounter report producer readiness.
- Completed and confirmed: P10 - Future PvP and siege report placeholders.
- Completed and confirmed: P11 - Reports Center prototype UI smoke and blocker report technical checkpoint.
- Completed and confirmed: Q1 - Notification domain models and mappers.
- Completed and confirmed: Q2 - Player notification read service and unread count.
- Completed and confirmed: Q3 - Staff notification read service and unread count.
- Completed and confirmed: Q4 - Notification bell / dropdown UI.
- Completed and confirmed: Q5 - Mark read / dismiss notification actions.
- Completed and confirmed: Q6 - Optional online toast presentation for fresh notifications.
- Completed and confirmed: Q7 - Staff notification inbox/dropdown UI.
- Completed and confirmed: Q8 - Notification type/admin readability pass.
- Completed and confirmed: Q9 - Notification hook diagnostics/admin readability.
- Completed and confirmed: Q10 - Notification smoke and hook verification technical checkpoint.
- Completed and confirmed: R1 - PvP domain/RPC type foundation.
- Completed and confirmed: R2 - PvP mappers.
- Completed and confirmed: R3 - PvP player RPC service.
- Completed and confirmed: R4 - PvP metadata read layer.
- Completed and confirmed: R5 - Vicinity page route.
- Completed and confirmed: R6 - Vicinity navigation entry.
- Completed and confirmed: R7 - Vicinity target candidate state.
- Completed and confirmed: R8 - Vicinity target list UI.
- Completed and confirmed: HOTFIX-COMBAT-1 - DB-owned exploration combat resolver.
- Completed and confirmed: HOTFIX-COMBAT-2 - exploration combat result/reward/report display.
- Completed and confirmed: HOTFIX-COMBAT-3 - reuse Walking Dead timing UI for exploration combat.
- Completed and confirmed: HOTFIX-COMBAT-LIVE - exploration combat live DB/RPC runtime.
- Completed and confirmed: R9 - Vicinity eligibility reason display.
- Completed and confirmed: R10 - Start spy action.
- Completed and confirmed: R11 - Start attack action.
- Completed and confirmed: R12 - PvP runtime activity display.
- Completed and confirmed: R13 - Spy result read state.
- Completed and confirmed: R14 - Spy result UI.
- Completed and confirmed: R15 - Attack result read state.
- Completed and confirmed: R16 - Attack result UI.
- Completed and confirmed: R17 - PvP report integration.
- Completed and confirmed: R18 - PvP notification routing.
- Completed and confirmed: R19 - PvP admin overview.
- Completed and confirmed: R20 - PvP action lifecycle admin surface.
- Completed and confirmed: R21 - PvP targeting/protection balancer surface.
- Completed and confirmed: R22 - PvP travel/manual-window balancer surface.
- Completed and confirmed: R23 - PvP resource consequence balancer surface.
- Completed and confirmed: R24 - PvP XP/reward balancer surface.
- Completed and confirmed: R25 - PvP Prestige context admin surface.
- Completed and confirmed: R26 - PvP anti-abuse explainability surface.
- Completed and confirmed: R27 - PvP report producer admin surface.
- Completed and confirmed: R28 - PvP foundation diagnostic admin surface.
- Completed and confirmed with follow-up: S24 - PvP and spy equipment display alignment.
- Completed and confirmed: S25 - Item requirement admin/balancer surface.
- Completed and confirmed: S0 - Generated DB types alignment after item/equipment foundation.
- Completed and confirmed: S1 - Item and equipment domain models.
- Completed and confirmed: S2 - Item and equipment mappers.
- Completed and confirmed: S3 - Equipment RPC service.
- Completed and confirmed: S4 - Current equipment read state.
- Completed and confirmed: S5 - Equipment paperdoll UI.
- Completed and confirmed: S6 - Armory shelf read state.
- Completed and confirmed: S7 - Armory shelf UI.
- Accepted hotfix: HOTFIX-REWARD-AUTO-RESOLVE - reward/drop communication and manual combat auto-resolve wording.
- Completed and confirmed: S8 - Armory shelf management.
- Completed and conditionally accepted: S9 - Item detail / popover equipment data.
- Completed and confirmed: S10 - Item requirement display.
- Completed and confirmed: SPECIAL TASK - Item generation requirements admin editor.
- Completed and confirmed: S11 - Equip single item action.
- Completed and confirmed: S11 follow-up - Dashboard runtime combat stats and Hero Stats use `get_hero_dashboard_runtime_stats(...)`.
- Completed and confirmed: UI-DASHBOARD-02 - desktop-first dashboard page frame and card rhythm.
- Completed and confirmed: UI-DASHBOARD-03 - desktop-first hero banner identity and context block.
- Completed and confirmed: UI-DASHBOARD-04 - primary hero action strip with real route CTAs.
- Completed and confirmed: UI-DASHBOARD-05 - Base Stats card with runtime stats_json values, DB-backed labels, component split, mapper cleanup and normal text color utilities.
- Completed and confirmed: Health State frontend integration - shared `ActiveHeroVitalsState` drives dashboard/topbar DB-owned `currentHealth / maxHealth`, level and XP progress.
- Completed and confirmed: UI-DASHBOARD-06 - Derived Stats compact row list with DB-owned runtime rows and deferred item popover pending a stable item reference.
- Completed and confirmed: UI-DASHBOARD-09 - estate context uses real current address/district source, computed address state and no fake nearby range; District and `/game/vicinity` row action now live in UI-DASHBOARD-11 `World State`.
- Completed and confirmed: UI-DASHBOARD-10 - persistent state source matrix; UI-DASHBOARD-11 may only implement states with dashboard-safe read sources, while active exploration/step/challenge remain blocked/conditional until a dashboard-safe current difficulty source exists.
- Completed and confirmed: UI-DASHBOARD-11 - `World State` row-list under Derived Stats backed by estate building job countdown from DB timing, trials remaining from latest hero/server `trial` counter row, active state from pending combat effect state, unread reports, District and Vicinity action; no notifications, fake timers, guessed difficulty or fake empty rows.
- Completed and confirmed: UI-DASHBOARD-12 through UI-DASHBOARD-20 dashboard closeout - 12-15 remain cancelled/deferred, 16-18 follow-up ordering/stale-state cleanup is accepted, 19 removed the obsolete legacy derived display path, and 20 is completed by acceptance decision. Manual dashboard smoke remains user-side/pending; next UI backlog area is UI-ACCOUNT after commit.
- Completed and confirmed: UI-ACCOUNT-1 - Account Entry Shell cleanup separates existing hero entry from hero creation, shows standard-server District A free/capacity from the start-flow read model, keeps sandbox multi-hero selection compact and DB-backed, and avoids duplicating existing hero context beside the sandbox switcher. Manual visual smoke remains user-side/pending.
- Completed and confirmed: UI-ACCOUNT-2 - sandbox/test server entry exposes `Create new hero on this sandbox` beside `Enter game` only when the start-flow read model allows sandbox creation; standard servers with existing heroes keep enter-only behavior. Manual smoke passed.
- Completed and confirmed: UI-HERO-CREATION-1 - existing-account hero creation is now one account-side screen with selected server context, editable hero name, DB/read-model-backed origin carousel, creation summary and canonical create action. Player-facing copy is Polish/product-facing and avoids DB/backend/Angular/workflow wording. Manual visual smoke remains user-side/final confirmation.
- Completed and confirmed: UI-HERO-1 - `/hero/attributes` is accepted as the base-stat allocation screen with full-width game-shell layout, header summary card, compact stat rows, Allocation draft divider rows, current-only derived preview, canonical save/reset/cap/cost/no-Character-Points behavior, central `mapBaseStatSnapshots(...)` reuse in allocation/combat, and user-side manual smoke. Copy/i18n polish remains a later non-blocking pass.
- Completed and confirmed: UI-HERO-2 - `/hero/attributes` Derived preview now uses the DB-backed `get_hero_attribute_allocation_preview_manifest(...)` one-shot manifest with a descriptor-driven local interpreter for allocation draft deltas. Endurance previews Health and Defense, Strength previews DB-provided damage rows, future supported scalar rows with `draftDependencies` and allowlisted descriptors are interpreted without row-key branches, current-only/unsupported/unknown rows stay current-only, plus/minus remains local draft state with no per-click RPC, and user-side manual smoke passed.
- Completed and confirmed: UI-ITEMS-1 - `/game/armory` uses the shared dashboard equipment preview path, shows clear visible/owned/capacity/hidden inventory counts, renders shelves 10 -> 1 -> Unsorted with matching move target order, preserves Details/equip/bulk/move/vendor/guild behavior, and keeps requirements available through the existing Details popover. List-level type/slot/requirement summaries remain a future read-model enhancement. Manual visual smoke passed user-side.
- Completed and confirmed: UI-ONBOARDING-ADD-1 - `/auth/server-entry` now has a separate account/onboarding shell frame without GameTopbar/GameSidebar, with centered Mythsworn topbar, account context panel and distinct `Wejdź do gry` / `Stwórz bohatera` account navigation. The central enter-game selector/detail-card redesign is deferred to UI-ONBOARDING-ADD-2.
- Completed and confirmed: UI-ONBOARDING-ADD-2 - `/auth/server-entry` and `/auth/create-character` now render under the routed `AccountEntryLayout`; `ServerEntryPage` owns only entry states and the existing-hero selector, which uses `get_account_entry_hero_contexts` for server, hero, level and address and enters through `enterHeroContext(serverId, heroId)`. Manual smoke remains user-side before commit handoff.
- Completed and confirmed: UI-ONBOARDING-ADD-3 - `/auth/create-character` now starts existing-account hero creation with a compact server eligibility selector before the Hero Creation stage. The selector shows DB/read-model District A free/capacity in selected-server details, blocks full District A and standard existing-hero create flow, keeps sandbox/test creation available when `canCreateHero=true` without `blockReason`, and does not render hero name/origin/create mutation before handoff. User-side smoke confirmed the final layout correction; visual emphasis/copy/helper cleanup remain non-blocking follow-ups.
- Completed and confirmed with follow-up: UI-ONBOARDING-ADD-4 - after eligible server selection, `/auth/create-character` shows the Hero Creation stage with readonly selected-server context, `Imię bohatera`, the start-flow-backed origin carousel, creation summary with `Punkty postaci: 1000`, and canonical create action disabled while the hero name is invalid. Direct origin buttons/dots, stat allocation UI, server selector UI, account registration UI and technical backend/read-model copy are absent from this stage. User-side smoke accepted the functional ADD-4 scope; follow-ups are visual polish against the dashboard/game-shell baseline, carousel typography/color tuning, summary card polish and possible later `shared/carousel` rename/cleanup if it remains origin-specific.
- Completed and confirmed with follow-up: UI-ONBOARDING-ADD-5 - Hero Creation remains on the canonical `create_hero_start_flow` backend workflow, fresh creation still routes to in-game Stat Allocation, unknown post-create route actions stay blocked, and stale submit success/error responses are ignored when the submitted server/name/origin context changes. Follow-ups: split `CreateCharacterPageFacade` when the flow is next touched, align remaining `Nazwa bohatera` validation/toast copy to `Imię bohatera`, and keep ADD-6/ADD-7 docs/audit plus ADD-8 responsive/manual smoke separate.
- Completed and confirmed: UI-ONBOARDING-ADD-6/ADD-7 documentation batch - origin content/read-model audit now records `get_start_flow_origin_options()` as the source for labels/descriptions/sort/active state and `entity_bonuses` bonus display, with artwork still a transitional `origin_key` asset convention; accepted onboarding prototypes are archived/mapped as `mythsworn_server_select.html` and `mythsworn_origin_screen.html` through ADD-7 onboarding entries while the broader UI-CORE-11 mapping remains draft for review. ADD-8 responsive/manual smoke is postponed.
- Completed and confirmed: S12 - Unequip slot action.
- Completed and confirmed: S13 - Bulk equip action.
- Completed and confirmed conditionally: S14 - Preset domain service and `PlayerEquipment` -> `HeroEquipment` rename.
- Completed and confirmed: S15 - Preset management UI.
- Q4 pending manual smoke: check a real DB row for `estate.building_job.completed`; source `action_url` must be `/game/mansion`. If it still returns `ViewState`, fix DB/content producer source and do not add a frontend remap.
- Q5 pending manual smoke: open bell, mark read, dismiss and action link; real DB/RLS denied-action smoke only if suitable test data/access exists.
- Q6 pending manual smoke: basic bell UI can be smoke-tested, but full fresh-toast validation needs a real DB producer notification. Eligible unread fresh rows should toast once; read rows and `default_toast_enabled = false` rows should remain inbox-only.
- Q7 pending manual smoke: real staff user + selected server + seeded/generated staff notification row; verify DB/RLS visibility and real staff action URL content.
- Q8 pending manual smoke: `/admin/notification-types` with real admin/operator access, seeded `notification_types` plus `notification_type_admin_section`, admin navigation card/link, and confirmation that no Save/Edit/Delete/Create controls are exposed.
- Q9 pending manual smoke: `/admin/notification-hooks` with real admin/operator access and DB rows from `get_admin_notification_db_owned_producer_diagnostics`; confirm producer rows, per-row blockers if any, and `game_report_created_is_not_default_notification_producer` as an explicit non-producer.
- Q10 pending manual smoke remains user-owned: player inbox load/count/mark-read/dismiss, staff selected-server inbox list/count/RLS, `/admin/notification-hooks` live rows, real producer smoke for building completion plus trade/auction/anti-abuse where data exists, and confirmation that report creation does not create a default notification.
- P2-P11 follow-ups: full manual smoke for list/detail/public-route/combat-rendering/item-reference rendering/unread/remove, low-level combat report creation, reward-drop attachment, contextual trial/encounter/PvP/siege readiness and deletion semantics needs authenticated session plus representative report producer/combat result/item/contextual/multi-access data.
- O6 user smoke confirmed real job behavior: start job, active job panel, route leave/return, browser refresh during an active job, completed-job settlement, and the new building level after `completes_at`.
- O8 user smoke confirmed building admin save/read behavior for `startingLevel = 0`. The editor now treats `starting_level = 0` as a legal not-built definition state, preserves it through mapping/form/save payload, keeps `startingLevel = 2` round-tripping, exposes `min=0`/`step=1`, and rejects negative/non-integer values instead of silently clamping.
- O12 pending manual smoke: Codex completed only the technical checkpoint. User-owned manual validation remains for `/game/mansion` resources versus topbar, build start/active/completion states, `/game/vicinity` district/center/filter and relocation, `/admin/buildings` save/reload for starting level/costs/formulas/metadata, and notification DB smoke for building-completion rows/action URLs when representative data exists.
- O9/O10/O12 follow-up: resource production after destructive relocation remains a non-blocking DB/runtime consistency issue. After relocating to a new estate with level 0 buildings, `hero_resources.per_hour` for drachma may remain stale (for example `+18/h`) until a later build/finalize path recalculates production (for example `+12/h`). Check `relocate_hero_estate_to_empty_address(...)`, `settle_hero_runtime_state(...)`, `get_hero_estate_runtime_state(...)` and `refresh_hero_resource_production_rates(...)`; do not add Angular-side production recalculation or local override.
- O11 UI debt: vicinity still uses native selects / `ngModel`. Keep it as temporary accepted debt; future UI pass should move it to project/PrimeNG reactive-form patterns without adding PvP/spy/protection workflows to `EstateVicinityPageState`.
- R9 follow-up: real PvP data smoke should confirm DB metadata labels for the main target eligibility reason keys. If player-facing copy should be fully Polish, seed/adjust DB metadata or later local fallback copy rather than changing the RPC contract.
- R10 follow-up resolved by R11: PvP start action now uses a global `pendingAction` lock across attack/spy and targets.
- R12/UI-PVP-1 cleanup resolved: accepted `/game/vicinity` target selection is split into a thin route page, child components, feature-owned state/helpers and estate relocation workflow ownership; filters use reactive controls and the old unsafe candidate-list/range merge flow is not part of the accepted screen.
- PvP result/report follow-up: provisional attack/spy result screens and notification deep links were removed from Vicinity cleanup. Future PvP result/report UI must return as a separate Reports/PvP/combat task with fresh route, privacy, snapshot-display and smoke scope.
- HOTFIX-COMBAT-LIVE manual smoke pending: real combat Trial/Encounter, ensure session, refresh during combat, one Walking Dead click equals one `submit_combat_player_action(...)`, DB response updates HP/log/current actor/manifest, finalization updates result/reward path, and no duplicate submit after refresh.
- HOTFIX-COMBAT-LIVE accepted follow-up: participant mapping now consumes DB-shaped `healthCurrent` and `healthMax` / `health_max`, so UI HP labels should not show `N/D` for max HP when DB returns `healthMax`. Submit payload remains limited to `p_session_id`, `p_timing_input_json.positionPercent` and `p_request_id`; manifest, streak, green-zone and speed remain DB-owned.
- HOTFIX-COMBAT-LIVE follow-up: add a stale-context regression for completed live combat state after difficulty/hero/context changes, and consider wiring `get_combat_live_state(...)` into an explicit delta/recovery refresh path if idempotent `ensure_exploration_combat_session(...)` is no longer sufficient. If post-DB-H2 green zone or speed still do not change after hits, treat it as a DB manifest issue rather than adding a frontend workaround.
- Formula balance refactor follow-up: before the next larger item generation/formula balance task, split `ItemGenerationFormulaBalanceFacade` further. It remains about 447 lines after the accepted variable-contract fix and must not absorb more workflow responsibilities.
- N13 pending validation: representative XP producer smoke remains pending until a real XP-producing flow exists or is intentionally selected; user/manual validation remains separate from technical green checks and must not be claimed by Codex.
- N12 DB/content cleanup follow-up: replace remaining formula target `hero points` wording with `Character Points`; confirm and align stat upgrade default test context keys (`hero_level`/`stat_level` vs allowed `heroLevel`/`statLevel`); seed the currently reported progression UI metadata gaps. Do not hide these with frontend fallback labels or fallback contexts.
- N9 follow-up: if progression history receives more enrichment slices, consider extracting a dedicated enrichment service.
- N9 DB/RLS caveat: `/admin/level-up-stat-bonuses` empty state is acceptable when no rules are seeded or current admin context intentionally cannot see them; if rules exist and should be visible, treat it as a DB/RLS/query blocker rather than adding frontend fallback rules.
- N8 refactor follow-up: split `RewardProfilesPageState` further; after N8 it remains 317 lines and still mixes load, selection, forms, options and key sync, but this is not a blocker for accepted N8.
- N7 follow-up: wire progression history into a concrete screen later; when UI/route exists, Codex should provide a manual smoke checklist for the user and must not run manual or route smoke itself.
- N6 follow-up: decide AuthState/dashboard refresh behavior after `grantExperience(...)` at the first real XP producer integration.
- N5 UI follow-up: dashboard remains provisional and should be rebuilt in a later UI/UX pass; do not expand N5 with further polish.
- N3 UI polish follow-up: surface the first exact row-level stat upgrade cost error in the summary instead of only the generic `characterPointSummaryError()`.
- N1 visual smoke follow-up: manually check dashboard, auction/trade and anti-abuse penalty sections for layout regressions from longer `Character Points` labels; `/hero/attributes` was covered by UI-HERO-1 user-side smoke.
- S10 accepted follow-up: real item detail requirement smoke looked OK for the checked item, but fuller manual variants remain pending for an item without requirements, an item with met requirements and an item with unmet requirements. S9 regression smoke should continue to check Value, Item stats such as `Damage 2-9`, Bonuses and no console/Angular runtime errors. `PlayerArmory` is a cleanup candidate for the next larger armory touch; split item detail/read orchestration if the service grows further.
- SPECIAL TASK accepted follow-up: item generation requirements are now edited inside `/admin/item-catalog` for the selected base item, prefix or suffix. Manual smoke confirmed adding a Dexterity 6 requirement to the Demonic prefix and runtime item detail then showed Hero level 1 plus Dexterity 6 while Value, Damage and Bonuses still rendered. UI for the catalog Requirements section is provisional and needs a later UX pass; `BuildingRequirementsState` should be renamed/split into a neutral entity-requirements state on the next larger requirements touch.
- S11 accepted follow-up: dashboard runtime damage/defense/health/luck/crit/evasion/attack count and Hero Stats now come from `get_hero_dashboard_runtime_stats(...)`, including `stats_json`. `HeroDerivedStats` / local derived resolver remains a cleanup candidate because it still has active topbar and combat-demo usages outside the dashboard path.
- S12 follow-up: equipment journal label for shifted entries should be neutralized from `Already equipped` to `Shifted` or rely directly on the DB message before slot-rotation UX is finalized.
- S13-S19 follow-up: `armory-page.html` and `armory-page.ts` are still heavy even after extracting loadout preset management, preview, apply/update suggestion, vendor lifecycle sequencing and the accepted standy visual cleanup. The next larger Armory touch should extract bulk selection, equipment journal and stand item card concerns into smaller components or dedicated state. `HeroEquipment` is intentionally hero-scoped after the S14 rename, but it is already broad; S15-S18 keep preset UI workflow mostly outside `ArmoryPage`, while S19 keeps player lifecycle mutations in the Armory shelf state/vendor service path.
- S20 follow-up: on the next small anti-abuse/admin touch, check whether scrapped item recovery UI should split `canSearch` and `canRecover`. DB search allows anti-abuse triage/sanction roles to inspect recoverable items, while recovery itself requires sanction-management authority; current S20 uses one management gate for the whole page. Also avoid growing `ScrappedItemRecoveryState` further without splitting the workflow.
- S21 follow-up: `isPlayerUsableItemStatus(...)` now means runtime/equipment usable (`active`, `locked_trade`, `locked_auction`) rather than vendor/listing/scrap eligible. Do not broaden it into lifecycle action eligibility. If touched again, consider renaming it to `isRuntimeUsableItemStatus(...)` or adding a clearer domain-specific helper.
- S22 follow-up: later exploration/PvE UI copy pass can unify the mixed Polish/English live combat/loadout boundary text. No behavior change is needed.
- S23 follow-up: live combat logs/results can show safe weapon/attack-source labels only if DB live events or result detail expose them. Do not reconstruct or guess weapon labels in Angular.
- UI-ENTRY-22-5-FOLLOW-UP DB/RPC/Migrator follow-up: expose a canonical pre-submit hero-name availability contract for selected server + proposed hero name so Angular can validate on blur/debounce. Until then, Angular must rely on `create_hero_start_flow` final validation and must not add direct `hero` table queries or client-only uniqueness checks.
- UI-COMBAT-4 follow-up: backend/Migrator owns combat resolver/read-model issues such as successful hit events with `0 obrażeń` and runtime stat discrepancies between dashboard, preview and combat. Angular must not correct these locally.
- UI-COMBAT-4 UI follow-up: if the accepted active/completed combat log row structure repeats, extract a shared combat/report log row pattern instead of reintroducing local component styling.
- UI-COMBAT-5 follow-up: add a shared outcome surface tone pattern for success/danger/warning result banners if the UI system gets that global pattern; do not add feature-local combat report surface CSS.
- UI-COMBAT-5 follow-up: make combat participant display models image-ready, using hero origin images when available and a PvE opponent fallback such as `public/images/warrior.png`; do not expand `initials()` into the final portrait solution.
- UI-COMBAT-5 follow-up: apply the same shared combat/report presentation pattern in Reports Center.
- UI-COMBAT-5 backend follow-up: backend/Migrator owns combat stat drift and any incorrect successful-hit `0 obrażeń` semantics; Angular must not correct these locally.
- S24 follow-up: PvP attack/result/report Prestige display must remain non-numeric until a dedicated player-safe Prestige summary read-model contract exists. Do not expose raw Prestige points, numeric deltas or projected deltas in player-facing PvP UI, and do not calculate Prestige in Angular.
- S26 scope note after S25: S25 already exposed and edits quality `requirementMultiplier` separately from value/bonus `multiplier` in the balance quality path. In S26, avoid repeating that work and focus only on remaining quality alignment gaps, if any.
- Epic N DB sanity follow-up: verify actual `balance_formula_targets`, `balance_formulas` and assignment rows for `hero_experience_to_next_level`, `hero_stat_upgrade_cost` and `hero_stat_level_cap`; generated TypeScript confirms schema/RPC signatures, not seeded content.
- M12 follow-up: apply `AdminReasonPresetField` to the remaining M12 reason fields for consistency.
- M12 follow-up: broader UI polish for white/native-looking inputs where global PrimeNG/theme styles are inconsistent.
- M12 follow-up: optional later refactor of large shared combat opponent files (`combat-opponent-admin.ts`, `combat-opponent.model.ts`).
- M12 follow-up: future UX/configurator pass can further improve tabbing/section layout; current M12 is functionally accepted.
- Epic O follow-up: fully integrate central `entity_requirements` / `requirement_definitions` into the building UI/read model. The M9-adjacent compile-only hotfix removed active legacy `building_requirements` reads and leaves building runtime requirements as an empty placeholder until the proper O slice.
- M2 follow-up debt resolved by UI-COMBAT-3: the temporary `/game/combat` sandbox/prototype flow and `combat-sandbox.model.ts` were removed after live combat integration moved the target UI to Exploration/Trial combat.
- M4 follow-up debt: first opponent catalog UI should add readable labels for manual item-generation references and generated bucket references, and should distinguish "families exist but no opponent definitions" from a fully empty opponent catalog.
- M5 follow-up debt: generated opponent equipment with `generatedBucketProfileId` is currently reported as an unsupported integration gap because the current item generation catalog loader does not support opponent-specific bucket profile selection; revisit with the first opponent catalog/admin UI or item generation catalog integration pass.
- DB cleanup candidate: `hero_derived` is no longer the frontend/runtime source for derived stats; replacement path is runtime derived stats from base stats, equipment, bonus templates/entity bonuses, derived stat definitions and formula assignments. Removal safety remains unknown until a dedicated SQL/reference audit and type regeneration.
- L12c refactor debt: exploration encounter forms/templates remain fairly manual; future refactor work should revisit FormFieldConfig/shared renderer/form factory direction with a strong reuse check before new helpers/configs.
- L11c refactor debt: trial and encounter configurators now share similar metadata facade, form rules and workflow patterns; do not refactor retrospectively in this task, but future work should check existing feature/core patterns before adding helpers.
- H17+ planning note: status/verdict/sanction/CP penalty action sections now share a similar workflow-action shell; before adding the next similar audited status-action section, check whether to extract a light shared wrapper/state/helper for error/success/loading/submit card layout and stale-guard handling.
- Reporting rule: future task reports must include a short Shared/reuse check covering reused shared/admin components, checked-but-not-reused options, and any new component justification.
- G6 follow-up planning note: remaining gameplay audit slices are major item operations, trade operations once frontend flows exist, and estate/building irreversible changes.
- Epic F direction: legacy bonus model retirement; new app paths should use dictionaries, semantic bonus_templates, and entity_bonuses.

## Codex Backlog Workflow

- Use one backlog task per Codex prompt unless the user explicitly groups tightly related tasks.
- Codex reads the required project docs before making changes.
- Codex reports the exact changes, verification result, and acceptance-criteria status after the task.
- For UI/manual smoke reports, Codex includes both the clicked UI path and the domain meaning of the action, following `docs/AGENTS.md`.
- The user confirms whether the task works.
- Only after user confirmation, Codex updates `current-state-summary.md`, `current-todo.md`, and any relevant task status/docs.
- Unconfirmed work must stay out of the completed-state summary.
- After confirmation, Codex prepares a commit message and waits for the next task instruction.
- Non-blocking UI/UX findings should be recorded in `docs/ui-ux-notes.md` unless they are promoted to task acceptance criteria.

## Highest Priority Gameplay TODO

### Exploration + trials loop
- Next: fix the backend/RLS blocker for player exploration challenge/reward reads: `/game/exploration` currently hits `permission denied for table hero_exploration_challenge_attempts`. Resolve with player-safe SELECT/RLS or a safe RPC/read model; do not bypass this in Angular with direct writes or service-role access.
- After the backend/RLS fix, repeat gameplay smoke: active difficulty tiers visible, debug `add_hero_remaining_actions` increases remaining trials for the selected hero/date, start exploration works, and challenge/reward read path does not throw permission denied.
- L9 has added the server-scoped admin exploration debug page with hero search, DB-backed selectors/pickers and sandbox helper RPC actions. Frontend/admin-debug smoke is accepted; full gameplay smoke is blocked by the backend/RLS issue above.
- L10 has added `/admin/exploration-lab` as a non-mutating balancing and explainability lab for exploration preview/simulation RPCs, with DB-backed selectors, labelled inputs, RPC explanations and simulation summary/distribution.
- L11 has added `/admin/exploration-trials` for admin/balancer trial definitions and combat candidate configuration. Mutations use `upsert_trial_definition`, `upsert_trial_combat_candidate`, and `deactivate_trial_combat_candidate` with mandatory reasons; no direct Angular writes to trial definition/candidate tables.
- L8 has added read-only persisted reward display for completed challenge attempts: challenge attempts -> reward grants -> reward grant entries -> items.
- L7 has added active challenge attempt UI with prototype manual completion and auto-resolve through canonical challenge RPCs. Full challenge/minigame smoke remains pending real trial/challenge data.
- L6 has added resolved step outcome display driven by DB `resolve_hero_exploration_step(...)` snapshots without frontend reroll or reward generation.
- L5 has added step timer/progress display from DB `resolves_at`/step status plus ready-only `Check result` handling through `resolve_hero_exploration_step(...)`.
- L4 has added graph/path display and valid direction choices from `get_hero_exploration_state(...)`, with movement started through `start_hero_exploration_step(...)`.
- L3 has added `/game/exploration`, DB-backed difficulty tiers, selected-difficulty state loading, and `start_or_get_hero_exploration(...)` start/status flow.
- Continue building the real exploration step loop instead of placeholders.
- Implement step outcomes:
  - nothing
  - light combat encounter
  - small resource encounter
  - trial appearance
- Implement progressive anti-dry-streak trial chance.
- Ensure normal encounters do not reset trial progression.

### Trial lifecycle
- Implement trial appearance separately from trial manifestation.
- Implement manifestation chance based on:
  - difficulty
  - relevant stat
  - smaller luck contribution
- Implement trial completion logic after manifestation.
- Add daily trial caps.
- Add premium-based attempt increase without changing quality/luck odds.

### Combat evolution
- Reuse the current Walking Dead duel slice in broader combat contexts:
  - light encounter combat
  - trial combat
  - future PvP combat
- Extend the formula-driven combat layer beyond the current targets:
  - initiative / turn order
  - multi-attack / weapon profiles
  - ranged specifics
- Decide which combat stats are purely derived and which can be modified directly.
- Add reward/death/outcome hooks for encounters and trials.

## Item and Reward TODO

### Difficulty-tier reward model
- Implement easy / medium / hard difficulty loop.
- Gate `Outstanding` item quality to the highest difficulty tier in actual reward generation.
- Preserve medium as the best all-around progression tier for many players.
- Prevent hard from becoming the always-correct farm mode too early.

### Luck integration
- Use luck in opportunity shaping, not guaranteed outcomes.
- Integrate luck into:
  - trial manifestation support
  - item bucket shaping
  - affix / quality variance
  - worst-outcome suppression at very high values
- Keep diminishing returns and opportunity-cost constraints.

### Item gameplay loop
- Connect generated items to exploration/trials.
- Expand item requirement consequences in live gameplay.
- Implement selling / scrapping effects on real item cleanup and economy.

## Buildings / Estates TODO

### Building execution
- O6 implemented player build/upgrade start through canonical `start_estate_building_upgrade(...)` and settled mansion runtime reads through `get_hero_estate_runtime_state(p_hero_id)`.
- Keep cost spending, resource materialization, build timers, active/recent jobs and completed level changes backend/RPC-owned; do not reintroduce Angular direct writes or local resource authority.
- Future building execution work should cover product-approved follow-up flows only, such as cancel/admin correction/claim UX if those become explicit backlog tasks.

### Estate progression
- Implement claiming / occupying a new empty estate.
- Enforce relocation consequences:
  - current buildings lost
  - strategic but easy-to-execute UX

### Siege and takeover
- Design staged siege flow:
  - preparation
  - participation
  - resolution
- Implement address swap logic for successful takeover.
- Define how guild support affects siege resolution.

## Social Systems TODO

### Prestige / reputation
- Create schema and runtime rules for prestige / reputation.
- Keep it separate from character level.
- Reward meaningful victories more than farming weak targets.

### Guilds / politics
- Add guild domain model and basic membership logic.
- Later add:
  - support structures
  - coalitions
  - district influence
  - leadership / voting systems

## Economy TODO

### Trade / auctions frontend
- Direct trade and one-item auction player-facing surfaces exist; continue with smoke/data hardening when sandbox data is ready.
- Manual smoke direct trade create/respond/confirm/cancel/reject once sandbox data includes two heroes, active items, a session and a real trade flow.
- Manual smoke gameplay auction listings, bids, buy now, cancellation, and closing once sandbox data includes active items, at least two heroes/users, Character Points and a real auction flow.
- Use existing RPC/domain operations for trade and auction mutations instead of direct table writes.
- Keep trade/auction lifecycle audit DB-owned through canonical RPCs/triggers; do not add Angular `AuditWriter` calls for these flows.
- Keep player-to-player trade based on Character Points.
- Keep drachmas as system/vendor currency/resource unless a later decision changes that.
- Hide or block trade/auction locked items from usable/equippable armory views.

### Trade Routes integration
- Replace `trade_active_offer_limit_fallback` with real Trade Routes/building bonus runtime.
- Decide how Trade Routes level affects active direct offers and other market limits.
- Keep the first Trade Routes integration simple and configurable.

### Character Points economy
- Connect Character Point earning to experience gain paths where appropriate.
- Use `character_point_ledger` for all persistent Character Point balance changes.
- Replace the current attribute-allocation direct balance update with the proper Character Point ledger/RPC flow.
- Keep vendor scrap outside player trade and Character Points; frontend core service uses `vendor_scrap_hero_item(...)`, while full player-facing vendor sell smoke waits for inventory/armory UI.

## Formula / Admin TODO

### Formula UX
- Consider moving function guides and templates to fully data-driven DB-backed configuration if admin ownership of these becomes important.
- Decide whether charting should stay lightweight SVG or be upgraded later.
- Add more domain-specific formula targets as gameplay systems come online.

### Balance coverage
- Continue replacing page-local hardcoded row editors with config-driven patterns where they are repetitive and stable.
- Keep shared non-component definitions in `core`.

## Technical TODO

### Testing
- Add targeted tests for:
  - combat simulator
  - formula variable validation
  - item bucket generation
  - bonus scaling types

### Cleanup
- Keep pushing mapper/helper logic that is not truly domain-object behavior into `core/utils` or equivalent focused utility folders.
- Continue splitting large admin screens into smaller components and facades where it improves clarity without over-engineering.

### Database/documentation sync
- Keep `docs/database-current.md`, `docs/current-decisions.md`, `current-state-summary.md`, and this TODO updated when migrations or confirmed implementation materially change semantics.
- Regenerate/update `src/app/core/types/database.types.ts` whenever schema changes require it.
- Do not mark backlog tasks complete in state docs until the user confirms the task works.
- Add a dedicated database/RLS task before production reliance on `hero.id != auth.uid()`: update onboarding policies and server-aware hero ownership checks.

---

## Added high-priority preparation track — U0 and UX special tasks

After G-series work and before deeper H/admin/staff UI work, prioritize the following preparation:

1. Regenerate Supabase `database.types.ts` after U0-N4 Stage 1–2 migrations.
2. Update `database-current.md` with U0 roles/staff/moderation contracts.
3. Add Codex tasks for U0 role/staff audits and Special UX explainability audits.
4. Do not ask Codex to build user/staff management UI before regenerated types and `database-current.md` include:
   - staff scopes;
   - moderation actions;
   - staff management RPC;
   - moderation history RPC;
   - staff assignment eligibility.
5. Track non-blocking role-aware UI and explainability findings in `ui-ux-notes` / current UX notes file.

### U0 DB foundation status

U0-N4 Stage 1–2 are structurally implemented in DB:

- staff scopes;
- moderation actions;
- warning/restriction/suspension/ban foundation;
- moderation history RPC;
- staff/user management RPC;
- permission helper split;
- access-control audit action types.

Remaining before frontend implementation:

- regenerate types;
- update docs;
- optionally run later behavioral tests with better test harness;
- implement runtime enforcement of restrictions in trade/auction/gameplay flows later.

